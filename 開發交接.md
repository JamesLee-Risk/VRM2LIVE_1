# 開發交接筆記

寫給**接手的下一個工作階段**。這裡只記錄「不看就會踩坑、或看程式碼看不出來」的事；
產品功能與使用方式請看 [README.md](README.md)，需求定義請看 [VRM2LIVE_規格需求書.md](VRM2LIVE_規格需求書.md)。

最後更新：2026-08-30（第二次：新增 FR-02-D 半身／全身追蹤）

---

## 1. 目前進度

已完成 **里程碑 M2（能播）** 與 **M2.5（能動全身）**。已實作／未實作的完整清單見 README。

| 模組 | 狀態 |
| --- | --- |
| FR-01 模型管理 | 完成 |
| FR-02 追蹤（webcam / 滑鼠） | 完成 |
| FR-02-C 語音口型 | 完成，使用者已實測通過 |
| FR-02-D 半身／全身追蹤 | 完成，**使用者尚未實測**（見 §3） |
| FR-03 映射引擎 | 完成 |
| FR-05 表情 | 完成 |
| FR-06 動畫 | 完成（FR-06-12 錄製功能未做） |
| FR-07 熱鍵 | 完成 |
| FR-11 場景光照 | 完成 |
| FR-15 輸出 | 完成 |
| FR-13 音效 | **未做**（`PlaySound` 動作已佔編號，觸發時提示未實作） |
| FR-16 手機串流、FR-18 多人協作 | **未做** |

---

## 2. 進行中：推上 GitHub

本機已 `git init`，`main` 分支有 1 個 commit（`62f3d30`），工作區乾淨，**尚未設定 remote**。

使用者已決定：**公開（public）**、由**使用者自行在 github.com 建立空 repo**，再由我們設定 remote 並推送。

還在等使用者回覆的三件事：

1. **repo 網址** — 建立時不要勾 README / .gitignore / License，否則會與本機 commit 衝突
2. **是否改用 GitHub noreply email** — 目前 commit 作者是 `ginyuch <ginyuch@gmail.com>`，
   公開後任何人都看得到。要改的話趁推送前 `git commit --amend --author=...`，推上去再改就得改寫歷史
3. **是否加 LICENSE** — 目前沒有，公開但無授權條款在法律上等同「保留所有權利」

環境現況：`gh` CLI **未安裝**（winget 在此工作階段需要互動確認，裝不了）；
Git Credential Manager 已安裝且設定於系統層級，第一次 `git push` 會跳出瀏覽器讓使用者登入。

### 授權地雷（重要）

`.gitignore` 排除**所有** `*.vrm`，不只 `Models/` 底下。
專案根目錄的範例模型 `白_shiro_.vrm` 授權是 `Redistribution_Prohibited`（禁止再散布），
推上公開儲存庫即構成再散布。**不要放寬這條規則。**
測試用動畫（`Animations/*.vrma`、`*.bvh`）也未進版控，需要時執行 `npm run make-anims` 產生。

---

## 3. 未解決的問題

**身體追蹤（FR-02-D）只做過合成資料驗證，尚未有人對著鏡頭實測。**
自我測試以合成地標走完「量測 → 解算 → 套用 → 讀回骨骼世界座標」全程（FR-02-61 等 7 項），
純邏輯測試另有 46 項，但**沒有任何一項證明對著真人攝影機的結果是對的**。
交給使用者測試時要請他確認三件事，這三件都是數值檢查抓不到的：

1. **舉手的是畫面上的同一側嗎**（鏡像；若相反，改的是 bodysolver.js 的 `SIDE`／`HAND_OF`，
   **兩張表要一起改**，只改一張會變成「側邊對了但手臂彎曲方向相反」）
2. **手指的彎曲方向對嗎**（若整隻手掌翻面，問題在 `palmBasis` 的 across 向量取向）
3. **手腕會不會過度扭轉**（「手腕扭轉讓給前臂」滑桿，預設 0.5；調 0 即完全關閉）

另外，`full` 模式需要攝影機拍得到整個人；坐在桌前只拍到上半身時，腿部資料會是雜訊，
應請使用者改用 `half`。

**右鍵拖曳的旋轉方向尚未經使用者確認。**
使用者回報右鍵／中鍵拖曳模型方向相反，已修正：攝影機移到 -Z 之後，畫面右方對應世界座標 **-X**，
所以水平位移要取負號。平移確定是對的；但「右鍵旋轉」有兩種合理慣例
（模型鼻尖跟著滑鼠跑 vs. 像繞著模型公轉），目前採前者。
若使用者說還是反的，**只翻轉 `tr.rotY` 的符號**，不要動平移。
位置在 [src/renderer/app.js](src/renderer/app.js) 的 `bindStageInteraction`。

---

## 4. 環境陷阱（會浪費大量時間的那種）

### 4.1 Electron 啟動不了

VS Code 的整合終端機會設定 `ELECTRON_RUN_AS_NODE=1`，這會讓 `electron.exe` 退化成純 Node，
症狀是 `TypeError: Cannot read properties of undefined (reading 'isPackaged')`。
執行前務必清掉：

```powershell
[Environment]::SetEnvironmentVariable('ELECTRON_RUN_AS_NODE', $null)
```

> 不要用 `Remove-Item Env:\ELECTRON_RUN_AS_NODE` — 這個工具環境的安全檢查會把
> `Env:\` 誤判為受保護路徑而擋下整條命令。

### 4.2 Node 是免安裝版

放在 `.tools/node`（未進版控），**不在 PATH 上**。每次要先加：

```powershell
$env:PATH = 'c:\Users\Admin\Desktop\VSCode\VRM2LIVE\.tools\node;' + $env:PATH
```

### 4.3 建置失敗會被安靜吃掉

`npm run build` 失敗時，如果你用 `| Select-Object -Last 1` 之類過濾輸出，
只會看到上一次成功的殘留訊息，而 `build/` 仍是**舊的 bundle**。
這曾經導致「改了程式碼但測試完全沒反應」的假象，查了很久。
**每次建置都要確認 exit code**，或至少看最後 6 行：

```powershell
& node scripts/build.mjs 2>&1 | Select-Object -Last 6
if ($LASTEXITCODE -ne 0) { "BUILD FAILED" }
```

### 4.4 PowerShell 與工具環境的雜項

- **沒有 heredoc**。要傳多行文字（例如 commit message）請先寫成檔案，再 `git commit -F <檔案>`。
- 命令列裡出現 `/\` 這種序列（常見於正規表示式字面量）會觸發路徑保護而被擋。
  解析自我測試結果請用現成的 [scripts/read-selftest.mjs](scripts/read-selftest.mjs)，不要在命令列寫 regex。
- PowerShell 直接輸出中文在此環境會變亂碼。要看中文結果，改用
  `node -e "..."` 或 `PYTHONIOENCODING=utf-8 python -c "..."`。
- 用 Bash 工具的 heredoc 傳 Python 程式碼時，字串裡的 `\n`、`\node`、`\face` 這類序列
  **會被當成跳脫字元處理掉**（曾經把 `.tools\node` 變成換行、`\f` 變成換頁字元）。
  寫檔案內容時請用 raw string `r'''...'''`，或乾脆避開反斜線與 `\n`。

---

## 5. 測試

四組測試，全部都應該是綠的：

```powershell
$env:PATH = 'c:\Users\Admin\Desktop\VSCode\VRM2LIVE\.tools\node;' + $env:PATH
[Environment]::SetEnvironmentVariable('ELECTRON_RUN_AS_NODE', $null)

node scripts/test-dsp.mjs        # 20 項 — 語音口型訊號處理
node scripts/test-hotkeys.mjs    # 47 項 — 熱鍵邏輯
node scripts/test-body.mjs       # 47 項 — 身體追蹤解算（合成骨架與地標）
node scripts/build.mjs

$p = Start-Process -FilePath 'node_modules\electron\dist\electron.exe' `
     -ArgumentList '.','--selftest' -PassThru -NoNewWindow `
     -RedirectStandardOutput 'st.log' -RedirectStandardError 'ste.log'
$p.WaitForExit(180000) | Out-Null
node scripts/read-selftest.mjs st.log        # 加 --all 可列出全部項目
```

自我測試目前 **64–65 項全過**（項數視模型而定）（含 FR-02-D 的 9 項、FR-01-11 變換的 6 項、FR-01-13 搖動骨骼的 4–5 項；
FR-01-13e 只在模型含碰撞體時才執行）。

**已知的非問題：**

- 自我測試會出現 `WARN=2`，內容是「熱鍵 HTTP 觸發：權杖錯誤，已拒絕」。
  那是 FR-07-05 的權杖驗證測試**故意**送錯權杖，被正確拒絕。不是 bug。
- `Models/白_shiro_.vrmlive.json` 裡的 `transform` 有非零位移與縮放、`hotkeys` 有 30 幾筆，
  那是使用者自己拖曳模型與按過「為所有表情建立」留下的，不是程式亂寫。

自我測試會在 `Logs/` 產生 4 張截圖（全身、臉部閉口、臉部發 /a/、揮手中），
用來人工確認著色、口型與動畫。**存檔順序與程式碼順序不一定相同**（中間有非同步步驟），
看圖前先確認檔名時間戳。

---

## 6. 已知陷阱（程式面）

README 的「實作備忘」有四條，都是實際踩過的坑，接手前建議先看：
three.js `PropertyMixer` 的寫入跳過、`KeyboardEvent.code` vs `key`、
攝影機在 -Z 導致畫面右方等於世界 -X、以及重複建立 `AnimationClip` 會讓綁定失效。

這裡補充兩條 README 沒有的：

**自我測試的觸發與 boot 是兩條獨立時間線。**
主行程在頁面載入後固定延遲送出 `selftest:run`，而 `boot()` 是非同步的。
如果等 boot 完成才註冊監聽，boot 慢的時候事件就遺失、測試永遠不會開始，
症狀是**間歇性** timeout。已用旗標處理「先觸發、後就緒」，見 app.js 結尾。
之後若再看到間歇 timeout，先懷疑這類競態，不要先懷疑功能程式碼。

**數值檢查驗不出「畫面是對的」。**
這個專案吃過三次虧：模型背對鏡頭、材質全白（CSP 缺 `blob:` 導致貼圖靜默失敗）、
動畫看似播放但骨骼沒動。三次的數值檢查都是綠的。
**改動渲染相關的東西時，一定要看截圖**，或加一個會因該問題失敗的具體斷言
（例如 FR-06-02b 直接量上臂角度，並同時排除「基準姿勢 -68°」與「rest pose 0°」兩種假通過）。


### 6.1 打包的兩個地雷

**`directories.buildResources` 一定要改掉。** electron-builder 預設它是 `build/`，
而那正是本專案 esbuild 的輸出目錄。buildResources 的內容會被排除在應用程式之外，
不改的話打出來的安裝檔裡**沒有 renderer bundle**，執行起來是白畫面。
已在 package.json 指到 `packaging/`。

**`ROOT_DIR` 在打包後是執行檔所在目錄**（`app.isPackaged ? dirname(getPath('exe')) : 專案根`），
所以 NSIS 必須是 `perMachine: false`（安裝到 `%LOCALAPPDATA%\Programs`）。
若改成安裝到 `Program Files`，設定檔與日誌會因為沒有寫入權限而全部失敗。

驗證打包結果不要只看有沒有產出 .exe：把 `.vrm` 放進 `dist\win-unpacked\Models\`
再跑 `dist\win-unpacked\VRM2LIVE.exe --selftest`，53 項應該全過。
實測過一次，能抓到「資源沒打進去」這類問題。

### 6.2 兩份試用者回報的調查結論（含一個自己造成的回歸）

**回報 A：「模型不見了、讀取後沒有人物」（夕雨勳）**

根因有兩層，第二層是 v0.2.0 自己造成的：

1. 該模型高 **3.33 公尺**（正常 VRM 約 1.6）。取景預設寫死以 1.6 公尺人形為前提，
   使用者只好用滾輪縮到 0.32 倍再拖曳。
2. v0.2.0 把縮放支點折進了每幀的變換公式（`pos = t + (1−s)(p − t)`），
   等於讓存檔的位移 t 變成「**縮放前**座標」。0.32 倍的模型 t.y 會膨脹到 **−7.9**，
   而此時只要再動一點縮放，模型就會位移 `(s₂−s₁)·(t − p)` ≈ 每 0.05 倍就跑掉 0.46 公尺。
   模型因而被甩到鏡頭外。

已改為：位移永遠是**世界座標**，支點修正在 `stage.setScaleKeepingView()` 裡於
縮放當下調整位移完成。設定檔有第二次轉換（`scalePivot: 'camera' → 'world'`），
外加「載入後若整個落在鏡頭外就自動重新配置」與「首次載入依高度自動配置」兩道防線。
自我測試的 **FR-01-11e** 會連續縮放六次並斷言位移不膨脹——那正是這個回歸的反例。

**量測尺寸不能用 `Box3.setFromObject()`。** 它對蒙皮網格取
`geometry.computeBoundingBox()`，而該方法會把**所有 morph target 的極值**算進去。
表情多的模型會被量成好幾倍：實測 1.62 公尺的模型算出 **13.03 公尺**，
自動配置就會做出完全錯誤的決定。`getModelBounds()` 因此改為只取 position 屬性的極值。

**回報 B：「頭髮會飄，取消勾選碰撞體就好了」（AluMMD 等）**

不是程式問題，是模型的碰撞體尺寸。逐項量測（以下數字皆為實測）：

| 情境 | 最大上飄 |
| --- | --- |
| 完全靜止 5 秒 | 0.00 mm |
| 根節點抖動 ±1／±5／±15 mm | 0.20 / 2.62 / 3.71 mm |
| 60／144／240 FPS 擺頭 5 秒 | 17.2 / 16.6 / 16.5 mm（無更新率相依） |
| 軟頭髮（stiffness 0.05）+ 零重力，擺動後靜置 | 0.4 mm |
| **開關碰撞體的靜止高度差** | 夕雨勳 **228.3 mm**、白_shiro_ 11.3、AluMMD 16.4 |

`vrm.update(dt)` 的時序正確、dt 未失控、我們也沒碰任何 springBone 參數。
症狀不是「持續飄動」而是**靜止位置本身被墊高**：夕雨勳的頭部碰撞體半徑
**0.467 公尺**（另有兩個 0.288），把瀏海整整撐高 22.8 公分。
AluMMD 則有 **117／193 個搖動骨骼的根部陷在碰撞體內部**，最深 52.9 mm。

因此新增了**碰撞體半徑倍率**（0.2–1.5）。比直接停用好，停用後頭髮會穿過頭部。

順帶記一筆：`dragForce` 是每幀衰減，直覺上高更新率螢幕阻尼會變弱，
但 `stiffness * delta` 同時等比變小，兩者互相抵銷——**更新率不相依**，別再往這個方向查。

### 6.3 搖動骨骼參數的預設值陷阱

有試用者回報自備模型的頭髮往上飄。以範例模型逐項量測後，**程式端沒有找到問題**：

| 情境 | 最大上飄 |
| --- | --- |
| 完全靜止 5 秒 | 0.00 mm |
| 根節點抖動 ±1 mm／±5 mm／±15 mm | 0.20 / 2.62 / 3.71 mm |
| 60 / 144 / 240 FPS 下頭部擺動 5 秒 | 17.2 / 16.6 / 16.5 mm（無更新率相依） |
| 重力歸零 | 0.0 mm |
| 軟頭髮（stiffness 0.05）+ 零重力，擺動後靜置 4 秒 | 0.4 mm |

`vrm.update(dt)` 的呼叫時序正確（applySolved → render），dt 未失控，我們也沒有碰
springBone 的任何參數。**更新率不相依**這點值得記住：`dragForce` 是每幀衰減，
直覺上高更新率會變不穩，但 `stiffness * delta` 同時變小，兩者互相抵銷。

真正值得注意的是：範例模型 122 個關節的 `gravityPower` **全部是 0**
（VRMSpringBoneTool 匯出的預設值），而 three-vrm 的 `gravityPower` 預設也是 0。
沒有重力時頭髮只會被 `stiffness` 拉回**綁定姿勢**——模型的頭髮綁定姿勢若是張開或上翹的，
畫面上就會是「浮在半空、不往下垂」。這解釋了為什麼只有部分模型會出問題。

因此補上了 FR-01-13 的控制項（先前只有設定欄位、沒有任何程式讀取）。
`gravity` 刻意設計為**加法**：乘法對 `gravityPower = 0` 的模型完全無效。

**還沒有結論的部分：** 手上只有一個模型，上表所有情境都正常。要再往下查需要一個
實際會出問題的 `.vrm`。介面上的搖動參數摘要就是為了讓試用者回報時附上這些數字。

### 6.4 模型設定檔有兩次一次性轉換

`transform.scalePivot` 標記位移的語意，目前應為 `'world'`。歷史上有兩次轉換：

| 標記 | 寫入版本 | 語意 | 轉換 |
| --- | --- | --- | --- |
| 無 | ≤ 0.1.0 | 世界座標 | 直接標記為 world |
| `'camera'` | 0.2.0 | 縮放前座標 | `t·s + (1−s)·p` → world |
| `'world'` | 0.2.1 起 | 世界座標 | — |

兩次轉換都不改變畫面上的位置。這個鍵**不可**加進 `defaultModelConfig`——
`mergeDefaults` 會把舊檔也補上標記，新舊就分不出來了。

### 6.5 鏡像方向：頭與身體的對齊

試用者回報「身體跟頭剛好不同方向，一個有鏡像、一個沒有」。

兩條路徑的鏡射定義本來就不同：臉部拿到的是 MediaPipe 的**姿態矩陣**，鏡像＝把
yaw／roll 取負；身體拿到的是**地標座標**，鏡像＝x 分量取負＋左右歸屬對調。
兩者各自都是正確的鏡射，但基準朝向差半圈，同一個旗標套下去就會反向。

對齊的唯一開關是 bodysolver.js 的 **`INVERT_BODY_MIRROR`**（目前為 `true`）。
臉部側的方向已由使用者長期實測確認，所以對齊責任放在身體側。

**之後若還要調整，只動一個地方：**

| 症狀 | 改哪裡 |
| --- | --- |
| 頭與身體**互相**相反 | `INVERT_BODY_MIRROR` |
| 頭與身體**一起**反了 | tracker.js `_extractPose` 的 `sign` |

兩者互不影響，不要同時改。自我測試的 **FR-02-74c** 會斷言「切換鏡像時頭與身體同時翻面」，
`FR-02-74d` 則確認身體側沒有自己的第二個鏡像開關。

> 注意：`scripts/test-body.mjs` 的鏡像測試會跟著這個常數走。改常數就要一併更新那幾條期望值。

### 6.6 兩個「不是功能問題、是平台行為」的坑

**Chromium 會把背景視窗節流到停擺。**
追蹤與解算都跑在主視窗的 `requestAnimationFrame` 裡，OBS 瀏覽器來源只是被動接收
廣播結果。主視窗被別的視窗蓋住時，Chromium 停止供應動畫框架，輸出就整個凍結——
試用者回報的「VRM2LIVE 不在最上層，OBS 來源就不會動」。

修法是 main.js 的三個命令列開關加上 `webPreferences.backgroundThrottling: false`，
**四者缺一不可**，且命令列開關必須在 `app.whenReady()` 之前設定。

A/B 實測（自我測試 **FR-15-02c**，把視窗 `hide()` 1.5 秒再量幀數）：

| 設定 | 遮蔽期間幀數 |
| --- | --- |
| 停用開關 | **0 幀（0 FPS）** |
| 啟用開關 | 91 幀（約 61 FPS） |

這條檢查會直接重現該情境，改動視窗或渲染相關設定後務必看它有沒有變紅。

**Electron 的 `globalShortcut` 會攔截按鍵，不是「監聽」。**
被註冊的按鍵在**所有程式**裡都會失效。先前所有鍵盤熱鍵一律走全域註冊，
使用者只要綁一個 `1`，就再也無法在任何地方輸入 1，而且完全看不出原因。

現在熱鍵預設只在視窗聚焦時由 renderer 的 keydown／keyup 觸發（`bindLocalHotkeys`），
要系統層級才勾選逐熱鍵的 `systemWide`。兩個名詞不要搞混：

| 欄位 | 意義 |
| --- | --- |
| `hk.global` | **跨模型**保留，不隨模型切換失效（FR-07-07） |
| `hk.systemWide` | 註冊到**作業系統**，會攔截該按鍵（FR-07-01b） |

舊設定檔的熱鍵沒有 `systemWide` 欄位 → 視為 false → 自動停止佔用按鍵，
不需要遷移程式碼。本機觸發路徑必須排除輸入框（否則在熱鍵名稱欄位打字會一直觸發動作）
與 `state.capturingHotkey`（否則錄製綁定的當下就會執行該熱鍵）。

### 6.7 身體追蹤的三個坑

**MediaPipe 的 handedness 標籤以「輸入影像已鏡像」為前提。**
餵未鏡像的原始串流進去，`handedness` 回報的左右手是反的。改以「手腕地標離姿態地標
哪一側手腕較近」歸屬，見 bodytracker.js 的 `_detectHands`。

**姿態、手部、臉部三個 landmarker 各自要有遞增的 timestamp。**
同一個 landmarker 收到重複的 timestamp 會丟例外。三者各自呼叫 `performance.now()`，
不要共用一個變數。

**身體追蹤的骨骼寫入必須在逐軸映射「之前」。**
stage.js 的 `applySolved` 順序是：重設 rest → 基準 A-pose → 動畫層 → **身體層** → 逐軸映射（`+=`）。
身體層若放到映射之後，頭部追蹤的旋轉會被整個蓋掉。
另外身體層是以 `quaternion.slerp(目標, weight)` 寫入，而不是直接 copy——
此時骨骼上正好是基準待機姿勢，所以這個插值同時也是模式切換的淡接（FR-02-68）。

---

## 7. 下一步建議

依規格順序還剩：FR-13 音效、FR-16 手機串流、FR-18 多人協作、FR-06-12 動畫錄製。

曾向使用者提過一個順序上的考量：**先做 FR-06-12（錄製）** 可以讓使用者用這套軟體自己錄出
`.vrma`，補上他手邊沒有動畫檔、無法完整測試 FR-06 的缺口。使用者尚未選擇。

FR-02-D 完成後這個考量更成立了：使用者現在可以用半身／全身模式親自做出手臂與手指的動作，
錄下來就是完整的動畫素材，不必再去外面找 `.vrma`。

---

## 8. 使用者與協作慣例

- 溝通與程式註解一律用**繁體中文**；註解說明「為什麼」而非「做什麼」。
- 使用者以規格書驅動開發，會逐條指定 FR 編號。改動若偏離規格，要在規格書裡一併更新
  （例如 FR-07-01 已從「最多 2 鍵」放寬為「最多 2 修飾鍵 + 1 主鍵」，並新增 FR-07-09）。
- 使用者手邊沒有 `.vrma` / `.bvh` 素材，明確表示 FR-06 的進一步測試先跳過。
- 使用者要求新功能時會明確指定「先改規格書，再實作」。FR-02-D 就是這樣做的：
  規格書新增 FR-02-09、FR-02-60～79、附錄 A-6，並同步改掉 L-06／L-07 這兩條
  「不支援全身與手指」的舊限制。
- 使用者會實際執行並回報 bug（拖曳方向、小鍵盤熱鍵都是他測出來的），
  所以交付時要講清楚「哪些我驗證過、哪些需要你實測」。
