# VRM2LIVE 軟體規格需求書（簡易版）

**文件版本：** 1.0
**撰寫日期：** 2026-08-30
**產品代號：** VRM2LIVE
**文件性質：** 衍生規格書 — 以《VTube Studio 軟體規格需求書 v1.0》為藍本，改寫為以 **VRChat／VRM 3D 模型**為核心之精簡版直播軟體規格
**目標平台：** Windows 11（開發／執行機：Windows 11 Home、NVIDIA RTX 3060、已安裝 OBS Studio）

> **與藍本文件之關係**
> 本文件保留原 VTube Studio 規格書之需求編號體系（FR-xx），僅實作其中 **11 個模組**：FR-01、FR-02、FR-03、FR-05、FR-06、FR-07、FR-11、FR-13、FR-15、FR-16、FR-18。
> 核心差異：原產品**明確排除** VRM／3D 模型（原文件 §1.2、L-01）；本產品反之——**僅支援 VRM，不支援 Live2D**。因此所有與 Live2D 綁定之概念（ArtMesh、`.moc3`、`physics3.json`、`.exp3.json`、Cubism SDK）均已替換為 VRM 對應概念（glTF Node、BlendShape/Expression、SpringBone、Humanoid Bone）。

---

## 目錄

1. [簡介](#1-簡介)
2. [產品總覽](#2-產品總覽)
3. [系統架構](#3-系統架構)
4. [功能需求](#4-功能需求)
   - [FR-01 模型管理](#fr-01-模型管理)
   - [FR-02 臉部與身體追蹤](#fr-02-臉部與身體追蹤)
   - [FR-03 參數映射引擎](#fr-03-參數映射引擎)
   - [FR-05 表情系統](#fr-05-表情系統)
   - [FR-06 動畫系統](#fr-06-動畫系統)
   - [FR-07 熱鍵系統](#fr-07-熱鍵系統)
   - [FR-11 場景、背景與光照](#fr-11-場景背景與光照)
   - [FR-13 音效系統](#fr-13-音效系統)
   - [FR-15 輸出、擷取與錄製](#fr-15-輸出擷取與錄製)
   - [FR-16 手機至電腦串流](#fr-16-手機至電腦串流)
   - [FR-18 多人協作（LiveRoom）](#fr-18-多人協作liveroom)
5. [非功能需求](#5-非功能需求)
6. [外部介面需求](#6-外部介面需求)
7. [資料模型與設定檔](#7-資料模型與設定檔)
8. [限制、假設與範圍外項目](#8-限制假設與範圍外項目)
9. [開發里程碑與驗收準則](#9-開發里程碑與驗收準則)
10. [附錄](#10-附錄)

---

## 1. 簡介

### 1.1 目的

本文件描述 **VRM2LIVE** 之功能與非功能需求，作為以下用途之依據：

- 工程實作（單人或小團隊即可於一台 Windows PC 上完成）
- 撰寫測試計畫與驗收條件
- 界定「簡易版」之功能邊界，避免範圍蔓延

### 1.2 範圍

VRM2LIVE 是一套以 **VRM 3D 模型**為核心的即時虛擬形象驅動軟體。系統以網路攝影機或智慧型手機追蹤使用者之臉部與身體，將追蹤結果映射至 VRM 模型之 BlendShape 與 Humanoid 骨骼，並將帶透明通道之渲染結果提供給 OBS 等串流軟體使用。

**明確納入範圍：** 模型管理、臉部／語音追蹤、以單一攝影機實現之半身與全身姿態追蹤（含手指）、參數映射、表情、動畫、熱鍵、場景背景與光照、音效、輸出擷取、手機串流、精簡版多人協作。

**明確排除於範圍外：**

| 項目 | 說明 |
| --- | --- |
| Live2D 模型 | 本產品**不支援** `.moc3` / Cubism SDK，屬 VTube Studio 之範疇 |
| VRM 模型之建模與綁定 | 屬 VRoid Studio、Blender、Unity + UniVRM 之範疇 |
| 推流編碼與平台上傳 | 屬 OBS Studio 之範疇 |
| VR 追蹤裝置與 IK 解算 | 本版之半身／全身追蹤以單一攝影機之視覺姿態估計實現（FR-02-D）；HMD／Tracker／慣性動捕服與 IK 反向運動學求解列為未來擴充 |
| 道具系統、Web Item、VFX、Twitch 整合、創意工坊、外掛 API | 對應原文件 FR-04／08／09／10／12／14／17／19／20／21／22，本版不實作（詳見 §8.3） |

### 1.3 名詞定義

| 名詞 | 定義 |
| --- | --- |
| **VRM** | 基於 glTF 2.0 的人形虛擬形象檔案格式，單一 `.vrm` 檔內含網格、材質、貼圖、骨架、表情與授權中繼資料 |
| **VRM 0.x / VRM 1.0** | 兩代規格。0.x 使用 `VRM` glTF 擴充與 `blendShapeMaster`；1.0 使用 `VRMC_vrm` 與 `expressions` |
| **VRM 設定檔** | VRM2LIVE 為每個模型產生之設定檔 `<模型名>.vrmlive.json`，人類可讀、未加密 |
| **Humanoid Bone** | VRM 標準化之人形骨骼（`hips`、`spine`、`chest`、`neck`、`head`、`leftEye`… 共 55 個定義，其中 15 個為必要） |
| **BlendShape / Expression** | VRM 的表情單元。一個表情群組（如 `happy`）可同時綁定多個網格 morph target、材質顏色與 UV 位移 |
| **MorphTarget** | glTF 網格層級的形變目標，為 BlendShape 群組的組成元素 |
| **SpringBone** | VRM 內建之次級動畫（搖動骨骼）系統，驅動頭髮、裙擺、飾品等 |
| **輸入參數（INPUT）** | 由追蹤系統、麥克風、滑鼠等來源產生之數值 |
| **輸出目標（OUTPUT）** | 可被驅動之模型端目標：表情權重、骨骼旋轉軸、或 LookAt |
| **MToon** | VRM 標準卡通渲染材質 |
| **VMC Protocol** | 基於 OSC 的動作資料交換協定，為 VRM 生態系事實標準 |
| **LiveRoom** | 本產品之精簡版多人同框系統（對應原產品 VNet） |

### 1.4 參考資料

- VRM 規格：`https://github.com/vrm-c/vrm-specification`
- VRM Animation（`.vrma`）規格：`https://github.com/vrm-c/vrm-specification/tree/master/specification/VRMC_vrm_animation-1.0`
- three-vrm（@pixiv/three-vrm）：`https://github.com/pixiv/three-vrm`
- MediaPipe Face Landmarker（含 52 項 ARKit 相容 blendshape 輸出）：`https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker`
- VMC Protocol：`https://protocol.vmc.info/`
- 藍本文件：[VTube_Studio_規格需求書.md](VTube_Studio_規格需求書.md)（同資料夾）

---

## 2. 產品總覽

### 2.1 產品定位

VRM2LIVE 是面向個人創作者的 **VRM 虛擬形象驅動軟體**，強調：

- **零硬體門檻** — 一支普通 webcam 即可運作，無需 iPhone、無需 VR 裝置、無需 RTX GPU
- **單機執行、無帳號、無雲端** — 所有運算與資料留在本機
- **與現有生態相容** — 直接使用 VRoid Hub／BOOTH 上既有的 `.vrm` 模型，並可接收 VMC Protocol 等既有追蹤來源
- **輸出即插即用** — 以 OBS 既有的來源類型接收，不自製擷取驅動

### 2.2 支援平台

| 平台 | 角色 | 支援層級 |
| --- | --- | --- |
| Windows 10 / 11（64 位元） | 主要渲染與串流端 | **完整支援（本版唯一目標）** |
| iOS / Android | 追蹤來源（經瀏覽器頁面或第三方 App） | 支援，但不提供原生 App |
| macOS / Linux | — | 本版不驗證（技術上可移植，見 §8.1） |

### 2.3 硬體需求

| 項目 | 最低 | 建議 |
| --- | --- | --- |
| CPU | 4 核心 | 6 核心以上 |
| GPU | 支援 WebGL2 / DirectX 11 之顯示卡 | 獨立顯示卡（開發機為 RTX 3060） |
| 記憶體 | 8 GB | 16 GB |
| 攝影機 | 任何 webcam | 1280×720、30 FPS 以上 |
| 麥克風 | 任何輸入裝置 | — |
| 其他 | — | OBS Studio 28 以上（本機已安裝） |

> **注意：** GPU 追蹤加速為選用；MediaPipe 於 CPU 模式亦可達 30 FPS。RTX GPU **非**必要條件（此為與原產品 NVIDIA Broadcast 追蹤器之關鍵差異）。

### 2.4 使用者角色

| 角色 | 說明 | 主要關注功能 |
| --- | --- | --- |
| **一般使用者（VTuber）** | 使用既有 VRM 模型直播 | 追蹤、表情、熱鍵、背景、輸出 |
| **模型師** | 製作 VRM 並交付客戶 | 表情對應、參數映射、設定檔攜帶性、授權旗標 |
| **協作參與者** | 參與多人同框 | LiveRoom 連線與安全性 |

### 2.5 參考實作技術堆疊（非強制）

需求本身與程式語言無關；下列為經可行性確認之建議組合：

| 分層 | 選用 | 理由 |
| --- | --- | --- |
| 應用外殼 | **Electron**（Node.js 20+） | 同時取得原生檔案系統／全域熱鍵／視窗控制，與 Web 渲染能力 |
| 3D 渲染 | **three.js + @pixiv/three-vrm** | 目前唯一同時完整支援 VRM 0.x 與 1.0（含 MToon、SpringBone、Expression、LookAt）之開源執行期 |
| 臉部追蹤 | **MediaPipe Tasks Vision — Face Landmarker** | 內建、免下載大型模型、輸出 52 項 ARKit 相容 blendshape，可直接對應 VRM 表情 |
| 語音口型 | **Web Audio API**（FFT + 共振峰分析） | 無外部相依即可實作五母音判別 |
| 網路 | **Node.js `dgram`（UDP／OSC）+ `ws`（WebSocket）** | 承接 VMC Protocol、iFacialMocap 與自製手機頁面 |
| 輸出 | **本機 HTTP 伺服器 → OBS 瀏覽器來源** | 瀏覽器來源原生支援透明通道，免寫 Spout2／NDI 原生外掛 |

**本機環境現況：** Python 3.10.6 已安裝；**Node.js 尚未安裝，為開工前置作業**。若改採 Python 路線（如 PyOpenGL + pygltflib 自行實作 VRM 執行期），需自行補齊 MToon 與 SpringBone，工作量顯著較高，故不建議。

---

## 3. 系統架構

### 3.1 邏輯分層

```
┌──────────────────────────────────────────────────────────┐
│  UI 層（模型選單、追蹤設定、參數映射表、表情面板、熱鍵面板）│
├──────────────────────────────────────────────────────────┤
│  控制層                                                    │
│  ├─ 熱鍵引擎（鍵盤／滑鼠／畫面按鈕／手機按鈕）             │
│  ├─ 表情引擎與表情群組                                     │
│  └─ 動畫播放引擎（含骨骼遮罩分層）                          │
├──────────────────────────────────────────────────────────┤
│  參數解算層                                                │
│  ├─ 輸入匯流（Webcam / 手機 / 麥克風 / 滑鼠）              │
│  ├─ 身體姿態解算（半身／全身，四元數直寫骨骼，FR-02-D）     │
│  ├─ 映射與平滑（IN→OUT 區間映射、限幅、平滑、自動呼吸/眨眼）│
│  ├─ 優先權仲裁（P0–P5 六級值提供者）                        │
│  └─ VRM 次級動畫（SpringBone，可獨立 FPS）                  │
├──────────────────────────────────────────────────────────┤
│  渲染層                                                    │
│  ├─ three.js 場景圖 + VRM 執行期（MToon 材質）              │
│  ├─ 攝影機控制（正交／透視、位置預設集）                    │
│  ├─ 背景（純色／透明／圖片／影片）                          │
│  └─ 光照（環境光 + 方向光 + 邊緣光）                        │
├──────────────────────────────────────────────────────────┤
│  輸出層（OBS 瀏覽器來源 / 視窗擷取 / PNG 截圖 / Spout2*）   │
├──────────────────────────────────────────────────────────┤
│  服務層                                                    │
│  ├─ 追蹤資料接收器（UDP：VMC / iFacialMocap；WS：手機頁面） │
│  ├─ 輸出用本機 HTTP 伺服器                                  │
│  └─ LiveRoom 房間伺服器／用戶端（WebSocket over TLS）        │
├──────────────────────────────────────────────────────────┤
│  持久層（JSON 設定檔、模型資料夾、備份、日誌）              │
└──────────────────────────────────────────────────────────┘
                                        * Spout2 為選用（優先級 C）
```

### 3.2 部署拓撲

- **拓撲 A（純 PC，預設）：** Webcam → 追蹤 → 渲染 → OBS 瀏覽器來源擷取
- **拓撲 B（手機追蹤 + PC 渲染）：** 手機瀏覽器或第三方 App → UDP／WebSocket → PC 渲染 → OBS
- **拓撲 C（外部追蹤來源）：** VSeeFace／Waidayo／iFacialMocap 等既有工具 → VMC Protocol → PC 渲染
- **拓撲 D（多人協作）：** 各參與者 PC ↔ LiveRoom 房間伺服器（僅轉發姿勢資料）↔ 各參與者 PC

### 3.3 每幀資料流

```
追蹤來源（10–60 FPS，不定）
   │ 內插至應用程式 FPS
   ▼
輸入參數匯流表（FacePositionX、EyeOpenLeft、VoiceA…）
   │ 逐項套用：區間映射 → 限幅 → 平滑
   │
   │   ┌─ 身體姿態解算（僅 half／full 模式，FR-02-D）
   │   │  地標 → 骨節方向 → 四元數（球面插值平滑）→ 逐骨骼姿態
   ▼   ▼
輸出目標暫存區 ── 優先權仲裁（P0…P5，高者覆寫，切換時淡接）
   │
   ├─→ 表情權重（VRM Expression / BlendShapeGroup）
   ├─→ 骨骼旋轉（Humanoid Bone，四元數合成）
   └─→ LookAt（眼球骨骼或表情）
   │
   ▼
表情之 Add／Multiply 修飾（最後統一套用：先全部乘、再全部加）
   │
   ▼
SpringBone 次級動畫求解 ──→ three.js 渲染 ──→ 輸出層
```

---

## 4. 功能需求

需求優先級定義：**M**（必要，Must）、**S**（應有，Should）、**C**（可有，Could）。
「簡易版」之驗收下限為**所有 M 級需求**；S 級為完整版目標；C 級為未來擴充。

---

### FR-01 模型管理

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-01-01 | 系統應於啟動時掃描 `Models/` 資料夾（含子資料夾），列出所有 `.vrm` 檔案為可用模型 | M |
| FR-01-02 | 系統應同時支援 **VRM 0.x**（glTF 擴充 `VRM`）與 **VRM 1.0**（`VRMC_vrm`），並於 UI 標示版本；兩者之表情與 LookAt 差異須由執行期抽象層吸收 | M |
| FR-01-03 | 系統應於首次載入模型時自動建立設定檔 `<模型名>.vrmlive.json`，與 `.vrm` 同目錄，格式為人類可讀之 JSON、**不加密** | M |
| FR-01-04 | 系統應解析並於「模型資訊」面板顯示 VRM 中繼資料：標題、版本、作者、聯絡資訊、參考來源 | M |
| FR-01-05 | 系統應解析並顯示 VRM **授權旗標**（0.x：`allowedUserName`／`commercialUssageName`／`violentUssageName`／`sexualUssageName`／`licenseName`；1.0：`avatarPermission`／`commercialUsage`／`modification` 等），並於載入時以摘要提示使用者 | M |
| FR-01-06 | 當模型之授權旗標為「禁止再散布」或「禁止商業使用」時，系統應於 LiveRoom 模型傳輸（FR-18）與截圖分享等出口顯示對應警語 | M |
| FR-01-07 | 使用者應可為模型設定顯示名稱、縮圖（`.png` / `.jpg`，建議 512×512；未指定時取 VRM 內建 `thumbnail`）與預設待機動畫 | M |
| FR-01-08 | 系統應提供模型選擇列，並提供「開啟模型資料夾」與「卸載模型」入口 | M |
| FR-01-09 | 系統應提供**自動設定（Auto-Setup）**：依 VRM 標準表情預設（`neutral`/`a`/`i`/`u`/`e`/`o`/`blink`/`blink_L`/`blink_R`/`lookUp`/`lookDown`/`lookLeft`/`lookRight`，1.0 另含 `happy`/`angry`/`sad`/`relaxed`/`surprised`）與 Humanoid 骨骼，自動建立完整輸入→輸出映射；載入新模型時應主動詢問是否執行 | M |
| FR-01-10 | Auto-Setup 應能處理**缺漏預設**之模型：如僅有 `blink` 而無 `blink_L`／`blink_R` 者，須自動退化為雙眼連動並於 UI 告知；LookAt 型態為 `Bone` 者須走骨骼路徑而非表情路徑 | M |
| FR-01-11 | 系統應提供**模型變換設定（Transform Config）**：位置 X/Y/Z、旋轉、縮放，以及「依頭部位置平移模型」之強度滑桿（後者由 FR-03 之 `FacePositionX/Y` → 根節點映射提供）。各項目除數值控制外，亦須可於畫面上直接操作：拖曳＝平移、右鍵拖曳＝旋轉、**滾輪＝縮放**、**Ctrl＋滾輪＝精細縮放**（步進約為滾輪單獨使用之四分之一）。縮放範圍 0.2×–4×，且**縮放須以攝影機注視點為支點**——以模型原點縮放時，被取景的部位會隨縮放上下位移，其幅度大於尺寸變化本身，使用者會誤認為「模型在上下移動」。支點修正必須在**縮放發生的當下**調整位移值完成，位移本身須維持**世界座標**語意；若把支點折進每幀的變換公式，位移值會變成「縮放前座標」而隨縮放倍率膨脹，縮小的模型再微調縮放即會被甩出畫面數公尺。**任一途徑之變換變更皆須即時同步至 OBS 輸出頁面**，不得只在主視窗生效 | M |
| FR-01-11b | 載入模型時應依其實際高度**自動配置縮放與位置**：取景預設是以身高約 1.6 公尺之人形為前提，而實際模型高度可由 0.5 公尺至 3.3 公尺不等，差異過大時畫面上將完全看不到人物。使用者已自行調整過之模型不得覆寫 | M |
| FR-01-11c | 若模型於載入後完全落在取景範圍外，系統應**自動重新配置**並明確告知，不得只呈現空白畫面——使用者無從分辨「模型在鏡頭外」與「模型讀取失敗」 | M |
| FR-01-11d | 模型尺寸之量測**不得**採用會納入 morph target 極值之邊界計算：表情數量多的模型會因此被量出數倍於實際的尺寸，連帶使自動配置做出錯誤決定 | M |
| FR-01-12 | 系統應提供攝影機設定：透視／正交切換、視野角（FOV）、取景預設集（全身／半身／臉部特寫），並可儲存為可由熱鍵呼叫之位置預設 | M |
| FR-01-13 | 系統應支援 **SpringBone**（VRM 0.x `secondaryAnimation` 與 1.0 `VRMC_springBone`），並提供下列全域控制：**硬度倍率**（0–2，預設 1）、**追加重力**（0–1，預設 0）、**阻尼倍率**（0–2，預設 1，結果限幅於 0–1）、**碰撞體半徑倍率**（0.2–1.5，預設 1）與**碰撞體開關**。碰撞體半徑倍率為必要項而非選用：實測模型之頭部碰撞體半徑可達 0.467 公尺，會把瀏海整片撐高逾 20 公分；直接停用碰撞體雖可解除該現象，卻會使頭髮穿過頭部。重力必須為**加法**而非乘法——多數匯出工具寫入的 `gravityPower` 為 0，乘上任何倍率仍為 0，而「頭髮停在綁定姿勢、不往下垂」正是零重力才會出現的症狀 | M |
| FR-01-13b | UI 應顯示模型**原始**搖動參數之摘要（關節數、碰撞體組數，以及硬度／重力／阻尼的數值範圍），使使用者回報搖動異常時可一併附上，免於在「模型設定問題」與「程式問題」之間反覆猜測 | S |
| FR-01-13c | 搖動骨骼設定須即時同步至 OBS 輸出頁面，且倍率一律以**模型原始值**為基準計算，不得將倍率反覆疊乘於前次結果之上 | M |
| FR-01-14 | 系統應支援**跨模型複製設定**，可選擇性複製：參數映射、表情設定、熱鍵、動畫指派、變換與攝影機預設 | S |
| FR-01-15 | 跨模型複製時，若目標模型缺少來源之表情或骨骼，應列出遺失項目清單供使用者確認，不得靜默丟棄 | S |
| FR-01-16 | 系統應在設定變更時立即存檔，並每 5 分鐘檢查一次，若有變更則將安全副本寫入 `Backups/` 資料夾（保留最近 10 份） | M |
| FR-01-17 | 當 `.vrmlive.json` 為唯讀時，模型仍應可載入，但不得寫入變更並須明確提示使用者 | S |
| FR-01-18 | 系統應對大型模型（>50 MB 或貼圖 >2048×2048）於載入時顯示記憶體與效能提示 | S |
| FR-01-19 | 系統應提供貼圖重新載入功能，支援外部改圖後即時套用 | C |

---

### FR-02 臉部與身體追蹤

#### FR-02-A 追蹤來源

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-02-01 | 支援 **Webcam 臉部追蹤**（MediaPipe Face Landmarker 或同級方案），輸出 468 點地標與 52 項 ARKit 相容 blendshape 係數 | M |
| FR-02-02 | 支援**麥克風語音口型同步（Lipsync）** | M |
| FR-02-03 | 支援**滑鼠位置**作為輸入參數 | M |
| FR-02-04 | 支援 **VMC Protocol（OSC over UDP）** 接收，可承接 VSeeFace、Waidayo、mocopi 相關工具等既有來源之骨骼與 blendshape 資料 | S |
| FR-02-05 | 支援 **iFacialMocap／FaceMotion3D 相容之 UDP 封包**，承接 iPhone ARKit 高品質追蹤 | S |
| FR-02-06 | 支援內建**手機瀏覽器追蹤頁面**（見 FR-16），使無 iPhone 之使用者亦可用 Android 手機作為追蹤來源 | S |
| FR-02-07 | 系統應可同時啟用多個來源，並提供**逐來源優先權**設定（如：手機臉部 > Webcam 臉部；麥克風口型永遠覆寫嘴部） | S |
| FR-02-08 | 不實作：遊戲控制器輸入、眼動儀、外部全身動作捕捉硬體（VR Tracker、慣性動捕服、光學動捕）。**手部與全身追蹤改以單一網路攝影機之視覺姿態估計實現，見 FR-02-D** | C |
| FR-02-09 | 支援 **Webcam 身體姿態追蹤**（MediaPipe Pose Landmarker 或同級方案）與**手部追蹤**（MediaPipe Hand Landmarker 或同級方案），詳見 FR-02-D | M |

#### FR-02-B 追蹤設定

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-02-20 | **隱私原則：** 系統不得於任何介面顯示 webcam 影像，且不得存在可顯示之程式碼路徑 | M |
| FR-02-21 | 系統應提供攝影機裝置選擇、解析度選擇與追蹤開關 | M |
| FR-02-22 | 應可獨立設定**追蹤 FPS** 與**應用程式渲染 FPS**；渲染應支援 VSync 與固定 FPS 上限 | M |
| FR-02-23 | 系統應將追蹤資料**內插至應用程式 FPS**，使 15–30 FPS 之追蹤來源在 60 FPS 渲染下仍平順 | M |
| FR-02-24 | 應提供**校準（Calibrate）**功能：以當前姿勢為中性原點，資料持久化至 `tracking_calibration.json`；校準亦可由熱鍵觸發 | M |
| FR-02-25 | 應提供「**追蹤遺失時行為**」設定：凍結於當前姿勢、或於 N 秒內平滑回復預設姿勢 | M |
| FR-02-26 | 應提供**眨眼連動**三種模式：永不連動、永遠連動（取左右平均）、頭部大幅轉動時連動（採用仍可見之單眼數值） | M |
| FR-02-27 | 應提供逐類別追蹤靈敏度調整：頭部旋轉、頭部位移、眼睛、眉毛、嘴巴 | M |
| FR-02-28 | 應提供**輸入死區（Dead Zone）**設定，抑制靜止時之微幅抖動 | S |
| FR-02-29 | 應提供追蹤狀態指示：目前來源、實際追蹤 FPS、是否偵測到臉部、延遲估計值 | M |
| FR-02-30 | 應提供**參數監看器**：以清單即時顯示所有輸入參數之原始值與平滑後之值，供除錯與映射調校 | M |
| FR-02-31 | 應提供 blendshape 逐項微調（Offset / Scale / Exponent），用以補償不同追蹤來源之強度差異 | S |

#### FR-02-C 語音口型同步

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-02-40 | 應提供麥克風裝置選擇與輸入音量表 | M |
| FR-02-41 | 應輸出 `VoiceA`、`VoiceI`、`VoiceU`、`VoiceE`、`VoiceO`、`VoiceSilence`、`VoiceVolume`、`VoiceFrequency` 八項參數 | M |
| FR-02-42 | 系統應確保 `VoiceA`–`VoiceO` **不會同時達到 1**，僅於較小數值區間混合 | M |
| FR-02-43 | 應提供音量增益（Volume Gain）、噪音閘（Volume Cutoff）、頻率增益（Frequency Gain）三項調整 | M |
| FR-02-44 | 應提供逐母音（A／I／U／E／O）之錄音校準流程與重設功能，並以頻譜視覺化回饋 | S |
| FR-02-45 | 應提供「一鍵對應母音」按鈕，將五母音自動映射至 VRM 之 `a`/`i`/`u`/`e`/`o` 預設表情 | M |
| FR-02-46 | 語音參數應可映射至**任意**輸出目標，不限於嘴部 | M |
| FR-02-47 | 應提供麥克風重新載入／靜音切換功能，並可由熱鍵觸發 | M |
| FR-02-48 | 應提供「口型來源」三選一：僅麥克風、僅攝影機、混合（取兩者較大值） | S |

---

#### FR-02-D 身體與手部追蹤（半身／全身）

> 本節取代原 FR-02-08 中「不實作手部追蹤與全身動作捕捉」之敘述。追蹤僅以**單一網路攝影機**之視覺姿態估計實現，不需任何外部動捕硬體、不需 VR 裝置、不需深度攝影機。

**追蹤模式**

| 模式 | 追蹤範圍 | 說明 |
| --- | --- | --- |
| `face`（**預設**） | 臉部 + 軀幹連動 | 與 FR-02-A／FR-02-B 之現行行為完全相同：僅追蹤臉部，頭部旋轉依附錄 A-3 之比例分配至 `neck`／`spine`／`chest`，其餘骨骼維持基準待機姿勢 |
| `half` | 臉部 + 腰部以上 | 於 `face` 之上追加：肩膀、上臂、前臂、手腕、**十指**、胸、腹、腰（脊椎鏈） |
| `full` | 全身 | 於 `half` 之上追加：髖、大腿、小腿、腳掌，以及（可選）根節點之垂直位移 |

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-02-60 | 系統應提供上表之三種身體追蹤模式，預設為 `face`；三者應可於追蹤進行中即時切換，不得中斷攝影機串流，亦不得重新初始化臉部追蹤 | M |
| FR-02-61 | `half` 模式應驅動下列 Humanoid 骨骼：`hips`、`spine`、`chest`、`upperChest`、`leftShoulder`／`rightShoulder`、`leftUpperArm`／`rightUpperArm`、`leftLowerArm`／`rightLowerArm`、`leftHand`／`rightHand` | M |
| FR-02-62 | `half` 模式應**額外以手部追蹤驅動十指**：每手 15 根手指骨骼（拇指 3 節、食指／中指／無名指／小指各 3 節），最多同時追蹤 2 隻手 | M |
| FR-02-63 | `full` 模式應於 `half` 之基礎上追加 `leftUpperLeg`／`rightUpperLeg`、`leftLowerLeg`／`rightLowerLeg`、`leftFoot`／`rightFoot`，並應可選擇是否同時驅動根節點之垂直位移（蹲下／起立） | M |
| FR-02-64 | 身體姿態應以**四元數直接寫入 Humanoid 骨骼**，不佔用 FR-03 映射表之項目——否則將與 FR-03-04「同一輸出目標僅能被指派一次」相衝突，且四元數姿態無法以逐軸區間映射正確表達 | M |
| FR-02-65 | 骨骼之基準方向須於模型載入時**自模型本身量測**（取正規化 rest pose 之骨節向量與左右肩連線建立基底），不得硬編碼座標軸方向；此為同時支援 VRM 0.x（預設朝 -Z）與 VRM 1.0（預設朝 +Z）之前提 | M |
| FR-02-66 | 身體姿態於優先權仲裁中屬 **P2（追蹤層）**：高於待機動畫（P1），低於一次性動畫（P3）與表情（P4）。一次性動畫依骨骼遮罩（FR-06-09）所接管之骨骼不得被身體追蹤覆寫 | M |
| FR-02-67 | 頭、頸與眼球之旋轉仍由臉部追蹤（FR-02-A）負責；身體追蹤**不得**驅動 `head`、`neck`、`leftEye`、`rightEye`，兩者須疊加而非互斥 | M |
| FR-02-68 | 模式切換須於可設定之時間內（預設 0.35 秒）平滑淡接；切回 `face` 模式時，受身體追蹤驅動之骨骼須平滑回到基準待機姿勢，不得瞬跳 | M |
| FR-02-69 | 應提供逐部位開關：軀幹、肩膀、手臂、手指、腿部、根節點位移；關閉之部位須即時淡出而非瞬間停止 | S |
| FR-02-70 | 應提供**獨立於臉部追蹤之身體取樣 FPS** 設定（預設 30），以控制姿態與手部估計之運算負載 | M |
| FR-02-71 | 應提供姿態模型精度二選一（輕量／標準），切換時即時重新載入模型檔而不重啟應用程式 | S |
| FR-02-72 | 應提供逐部位平滑度設定；平滑須以四元數**球面線性插值**實作，不得對尤拉角逐軸平滑（會產生萬向鎖與角度跨越 ±180° 時之翻轉） | M |
| FR-02-73 | 左右手之歸屬應以**姿態地標之手腕位置**就近判定，不得僅依賴追蹤函式庫回報之 handedness 標籤——該標籤以「輸入影像已鏡像」為假設，餵入未鏡像之攝影機串流時會左右顛倒 | M |
| FR-02-74 | 鏡像須為**單一設定**（FR-02-21），同時作用於臉部、身體與手部，**預設為開啟**。臉部與身體之轉向必須一致——不得出現「頭轉向一側、身體轉向另一側」。兩條路徑之輸入格式不同（臉部為姿態矩陣，鏡像即取負 yaw／roll；身體為地標座標，鏡像即 x 分量取負＋左右歸屬對調），基準朝向相差半圈，故實作上必須以**單一常數**對齊兩者，並以自動化測試守住「切換鏡像時兩者同時翻面」 | M |
| FR-02-75 | 身體或手部追蹤遺失時應依 FR-02-25 之「追蹤遺失時行為」設定處理（凍結於當前姿勢或於 N 秒內平滑回復），不得瞬間掉回基準姿勢 | M |
| FR-02-76 | 應提供身體追蹤校準：以當前姿勢為中性原點（修正攝影機仰角與坐姿造成之系統性偏移），資料併入 `tracking_calibration.json`（FR-02-24） | S |
| FR-02-77 | 模型缺少非必要骨骼（`upperChest`、`leftShoulder`／`rightShoulder`、腳趾、部分手指）時須自動退化：其份額併入最近之現存父骨骼，不得報錯或使整體姿勢崩壞（NFR-C-05） | M |
| FR-02-78 | UI 應以**獨立分頁**呈現本節功能，內容須包含：三種模式之切換按鈕（其中「僅臉部＋軀幹」為回歸預設之按鈕）、逐部位開關、各項滑桿，以及偵測狀態指示（是否偵測到身體／左手／右手、實際取樣 FPS） | M |
| FR-02-79 | 身體姿態須與臉部追蹤結果一併廣播至 OBS 輸出頁面；輸出頁面**不得**重跑姿態估計（FR-15-02） | M |

> **隱私（FR-02-20 / NFR-S-02）：** 身體與手部追蹤沿用臉部追蹤之同一份攝影機串流，同樣不得於任何介面顯示影像、不得繪製骨架疊層、不得將地標資料寫入檔案或傳出本機。
>
> **精度預期：** 單一攝影機之姿態估計對「深度」（沿鏡頭光軸之前後位移）本質上不精確，快速動作、肢體互相遮蔽、下半身位於畫面外或桌面下時精度顯著下降。UI 須以提示文字明確告知此限制（見 L-06、L-07）。

---

### FR-03 參數映射引擎

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-03-01 | 使用者應可將任一輸入參數自由映射至任一輸出目標 | M |
| FR-03-02 | 輸出目標應支援三種型態：**（a）表情權重**（VRM Expression／BlendShapeGroup，範圍 0–1）、**（b）Humanoid 骨骼旋轉**（指定骨骼 + X/Y/Z 軸，單位為度）、**（c）LookAt**（水平／垂直注視） | M |
| FR-03-03 | 輸入與輸出區間應可獨立設定；映射方向可為正向或反向（例：IN [0,1] → OUT [-15,15] 或 [15,-15]） | M |
| FR-03-04 | 同一輸出目標僅能被指派一次，避免多輸入寫入衝突；重複指派時 UI 須阻擋並提示既有指派項 | M |
| FR-03-05 | UI 應以即時指示點顯示當前數值於輸入與輸出區間中之相對位置 | M |
| FR-03-06 | 應提供「**限制範圍（Limit Range）**」開關，超出設定範圍時數值應被限幅並平滑收斂 | M |
| FR-03-07 | 應提供**逐映射平滑度**設定（0–100）；平滑度越高抖動越小但延遲越大，UI 須標示此權衡 | M |
| FR-03-08 | 應提供**自動呼吸（Auto-Breath）**：輸出目標以呼吸節奏來回淡入淡出（可調週期與幅度），不需輸入參數且忽略任何輸入 | M |
| FR-03-09 | 應提供**自動眨眼（Auto-Blink）**：以隨機間隔（可調平均間隔與變異）驅動眨眼表情，可與輸入參數併用（取較大值） | M |
| FR-03-10 | 輸入參數清單應依當前啟用之追蹤來源**動態顯示可用性**，不可用者須以灰階標示並註明所需來源 | M |
| FR-03-11 | 系統應實作六級**值提供者優先權**仲裁機制（見下表），並於控制權移轉時平滑淡接，不得產生跳動 | M |
| FR-03-12 | **骨骼旋轉合成規則：** 同一骨骼被多個來源驅動時（如動畫提供基礎姿勢、追蹤提供頭部旋轉），應以四元數依優先權由低至高依序合成，而非直接覆寫 | M |
| FR-03-15 | 身體追蹤（FR-02-D）之骨骼姿態不進入本映射表，亦不佔用 FR-03-04 之目標指派；使用者仍可對同一骨骼建立逐軸映射，其結果疊加於身體姿態之上 | M |
| FR-03-13 | 應提供映射預設集匯出／匯入（`.vrmmap.json`），使同一套映射可跨模型與跨機器共用 | S |
| FR-03-14 | 應提供「重設此映射」與「重設全部映射」，後者須二次確認 | M |

**值提供者優先權（由低至高）**

| 優先級 | 值提供者 | 說明 |
| --- | --- | --- |
| P0 | VRM 預設姿勢／表情零值 | 所有輸出目標皆具備 |
| P1 | 待機動畫（Idle Animation） | 循環播放 |
| P2 | 臉部追蹤／語音／滑鼠 | 經 FR-03 映射後之值 |
| P2 | 身體追蹤（FR-02-D） | 四元數直寫骨骼，與同層之逐軸映射疊加而非互斥 |
| P3 | 一次性動畫（One-Time Animation） | 僅於動畫播放期間 |
| P4 | 表情（Expression） | 僅於表情啟用期間 |
| P5 | SpringBone 次級動畫 | 最高優先，且僅作用於 SpringBone 所轄骨骼 |

> **加法／乘法例外：** 表情中設為 Add／Multiply 模式之項目不參與上述覆寫仲裁，而是在所有計算完成後統一套用；同一目標**先套用全部乘法，再套用全部加法**，最後依輸出型態限幅（表情權重限 0–1）。

---

### FR-05 表情系統

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-05-01 | 系統應列出模型內所有 VRM 表情群組（0.x 之 `blendShapeGroups` / 1.0 之 `expressions`），區分**預設表情**與**自訂表情** | M |
| FR-05-02 | 應可於應用程式內建立、編輯、刪除表情，並儲存至 `<模型名>.vrmlive.json`；**不得修改原始 `.vrm` 檔案** | M |
| FR-05-03 | 表情編輯器應可選取任意組合之：既有表情群組權重、個別網格 MorphTarget 權重、Humanoid 骨骼旋轉偏移 | M |
| FR-05-04 | 表情編輯器應提供即時預覽，並提供「與中性姿勢比較」切換 | M |
| FR-05-05 | 每個表情項目應支援三種模式：**Overwrite**（覆寫，預設）、**Add**（加法）、**Multiply**（乘法） | M |
| FR-05-06 | Multiply 模式應可用於反轉追蹤方向（倍率設為 -1）；多個乘法應連乘（如 0.5 × 0.5 = 0.25） | M |
| FR-05-07 | 多個來源寫入同一表情時，應採最後啟用者之值；停用後應回退至前一個仍啟用者，全部停用後回退至動畫／追蹤／預設值 | M |
| FR-05-08 | 應可在不建立熱鍵之情況下，於表情清單中直接以滑鼠開關表情 | M |
| FR-05-09 | 應支援表情之**淡入／淡出時間**（0–5 秒，預設 0.25 秒） | M |
| FR-05-10 | 應支援 VRM 1.0 之**覆寫旗標** `overrideBlink`／`overrideLookAt`／`overrideMouth`（值為 `none`／`block`／`blend`）；VRM 0.x 模型應提供等效之手動設定 | M |
| FR-05-11 | 應支援 VRM 1.0 之 `isBinary` 二值表情（權重僅取 0 或 1，用於貼圖式表情） | S |
| FR-05-12 | 應提供**表情群組（Expression Group）**：群組內某一表情啟用時，其餘自動停用（互斥） | S |
| FR-05-13 | 表情群組應支援：命名、整組啟用／停用、成員加入／移除、成員即時啟用狀態顯示、群組層級之淡入淡出時間（覆寫個別設定） | S |
| FR-05-14 | 表情群組應支援**反轉（Inverted）**成員：關閉時視為啟用、開啟時視為停用（用於「脫帽」「換裝」類表情） | S |
| FR-05-15 | 應提供「**清除所有表情**」動作，並提供逐表情之「不受清除影響」標記（於清單以 ⭐ 標示），用於長期外觀客製 | S |
| FR-05-16 | 應提供「為所有尚未設定熱鍵之表情自動建立熱鍵」之批次功能 | S |
| FR-05-17 | 表情應可設定顯示名稱（在地化），至少支援中文與英文兩種，並依介面語言顯示 | C |
| FR-05-18 | 應可將表情標記為「於清單中隱藏」，隱藏項目僅於開啟「顯示全部」時出現 | C |
| FR-05-19 | 所有表情客製資料應寫入 `.vrmlive.json`，可隨模型一併散布 | M |

> **與 Live2D 之關鍵差異：** VRM 表情群組本身即可同時綁定 morph target、材質顏色與 UV 位移，故無需原產品之獨立 ArtMesh 著色系統（原 FR-10）；材質類客製一律經表情達成。

---

### FR-06 動畫系統

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-06-01 | 系統應載入 `Animations/` 資料夾與模型同層資料夾中之動畫檔案 | M |
| FR-06-02 | 應支援 **`.vrma`（VRM Animation）** 格式，此為 VRM 生態系之標準人形動畫格式 | M |
| FR-06-03 | 應支援 **`.bvh`** 格式，並於載入時依 Humanoid 骨骼名稱進行重定向（Retarget） | S |
| FR-06-04 | 應支援**循環待機動畫**與**一次性動畫**兩種播放型態 | M |
| FR-06-05 | 應可設定每個動畫之淡入／淡出時間 | M |
| FR-06-06 | 應可設定動畫於 X 秒後結束、播放至結尾即結束、或於熱鍵放開時結束 | M |
| FR-06-07 | 應提供「**停在最後一幀（Stop on Last Frame）**」選項：播放結束後保持最後一幀之骨骼姿勢直到再次觸發 | S |
| FR-06-08 | 「停在最後一幀」**不得**影響已在 FR-03 中設為追蹤輸出目標之骨骼（如 `head`、`neck`），該類骨骼應於動畫結束後自動淡回追蹤控制 | M |
| FR-06-09 | 應提供**骨骼遮罩（Bone Mask）**：每個動畫可指定其作用範圍為「全身」「僅上半身」「僅下半身」「僅手臂」或自訂骨骼集合，未納入遮罩之骨骼維持由追蹤或其他動畫驅動 | M |
| FR-06-10 | 動畫播放時，SpringBone 應持續作用於其所轄骨骼並覆寫動畫值（見 FR-03 優先權 P5） | M |
| FR-06-11 | 應提供**姿勢（Pose）**功能：將當前骨骼姿勢儲存為單幀靜態姿勢，可如表情般開關 | S |
| FR-06-12 | 應提供**動畫錄製**功能，將當前追蹤與表情結果錄製為 `.vrma`，並以 60 FPS 為曲線基準 | S |
| FR-06-13 | 動畫錄製應提供「不錄製未變動之通道」選項，並於錄製後顯示逐通道開關與平滑滑桿 | C |
| FR-06-14 | 應支援動畫播放速度倍率（0.25×–2×）與鏡像播放 | C |

---

### FR-07 熱鍵系統

#### FR-07-A 觸發方式

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-07-01 | **鍵盤熱鍵：** 支援最多 2 個修飾鍵（Ctrl／Alt／Shift／Super）搭配 1 個主鍵。主鍵須以**實體按鍵位置**記錄，使小鍵盤數字與主鍵盤數字列可分別綁定 | M |
| FR-07-01b | 熱鍵預設**僅於應用程式視窗取得焦點時**生效；系統層級註冊須為**逐熱鍵的選擇加入**，預設關閉。理由：作業系統層級的快捷鍵註冊會**攔截**該按鍵，使其在所有其他程式中失效——使用者若綁定一個未搭配修飾鍵的字母或數字，將無法在任何地方輸入該字元，且完全無從察覺原因 | M |
| FR-07-01c | 啟用系統層級註冊時，UI 須明確告知該按鍵將被全系統佔用，並建議搭配修飾鍵或改綁小鍵盤 | M |
| FR-07-01d | 視窗聚焦時，於文字輸入欄位內打字**不得**觸發熱鍵 | M |
| FR-07-02 | **滑鼠熱鍵：** 支援左鍵、中鍵、右鍵（限應用程式視窗內） | M |
| FR-07-03 | **畫面按鈕熱鍵：** 最多 8 個，顯示於主畫面側邊之半透明圓形按鈕；可調整透明度、可完全隱藏、可逐鍵自訂顏色與標籤；**輸出畫面中不得包含此類 UI** | M |
| FR-07-04 | **手機按鈕熱鍵：** 手機追蹤頁面（FR-16）應提供 8 個按鈕，按下時將對應 ID 之熱鍵事件傳至 PC | S |
| FR-07-05 | **本機 HTTP 觸發：** 應提供 `POST /api/hotkey/{id}` 端點，供 Stream Deck、批次檔等外部工具觸發；**預設關閉**，啟用時須綁定 `127.0.0.1` 並要求權杖 | S |
| FR-07-06 | 熱鍵應可命名，觸發事件須寫入日誌以利除錯 | M |
| FR-07-07 | 熱鍵設定應儲存於模型設定檔中；另應支援標記為「全域熱鍵」，不隨模型切換而失效 | M |
| FR-07-08 | 熱鍵衝突（同組合鍵指派多個動作）應於設定時偵測並警告 | M |
| FR-07-09 | 全域快捷鍵註冊失敗（例如組合已被其他程式占用）須於介面明確回報，不得只寫入日誌——否則使用者只會看到「按了沒反應」而無從得知原因 | M |

#### FR-07-B 熱鍵動作清單

系統應支援下列 **15 種**熱鍵動作（原產品 23 種中，與本版範圍相關者，另加本版新增項）：

| # | 動作 | 說明 | 對應原產品 |
| --- | --- | --- | --- |
| 0 | `TriggerAnimation` | 播放一次性動畫（依骨骼遮罩覆寫追蹤） | 同 |
| 1 | `ChangeIdleAnimation` | 切換循環待機動畫（不寫入模型設定檔） | 同 |
| 2 | `ToggleExpression` | 開關指定表情 | 同 |
| 3 | `RemoveAllExpressions` | 清除所有表情（⭐ 標記者除外） | 同 |
| 4 | `TogglePose` | 開關指定靜態姿勢 | 新增（VRM 專屬） |
| 5 | `MoveModel` | 將模型變換與攝影機移至記錄之預設，可設定移動耗時與緩動曲線 | 同 |
| 6 | `ChangeBackground` | 切換背景 | 同 |
| 7 | `ChangeLighting` | 套用記錄之光照預設 | 新增（3D 專屬） |
| 8 | `ReloadMicrophone` | 重新載入麥克風，或切換麥克風靜音 | 同 |
| 9 | `CalibrateTracking` | 觸發追蹤校準 | 同（原 `CalibrateCam`） |
| 10 | `ChangeModel` | 切換 VRM 模型 | 同（原 `ChangeVTSModel`） |
| 11 | `TakeScreenshot` | 依先前截圖設定拍攝截圖 | 同 |
| 12 | `ToggleTracker` | 開關指定追蹤來源（webcam／手機／VMC） | 同 |
| 13 | `PlaySound` | 播放指定音效（見 FR-13） | 新增（原為熱鍵附屬設定） |
| 14 | `ChangeBodyMode` | 切換身體追蹤模式（僅臉部／半身／全身，見 FR-02-D） | 新增（本版專屬） |

> 原產品之 `ScreenColorOverlay`、`RemoveAllItems`、`ToggleItemScene`、`DownloadRandomWorkshopItem`、`ExecuteItemAction`、`ArtMeshColorPreset`、`ToggleTwitchFeature`、`LoadEffectPreset`、`ToggleLive2DEditorAPI`、`WebItemAction` 等 10 項，因對應模組不在本版範圍內而移除。

#### FR-07-C 熱鍵附屬設定

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-07-20 | 表情與動畫熱鍵應可個別設定淡入／淡出時間，覆寫該表情／動畫之預設值 | M |
| FR-07-21 | 應可設定「X 秒後自動停止」或「放開按鍵即停止」 | M |
| FR-07-22 | 熱鍵應可附掛音效播放（見 FR-13） | S |
| FR-07-23 | 每個熱鍵應可設定**冷卻時間**（0–60 秒），冷卻期間再次觸發須被忽略且不排入佇列 | S |
| FR-07-24 | 熱鍵應可設定為**串接動作**：單一熱鍵依序執行最多 4 個動作（如：播放動畫 + 套用表情 + 播放音效） | S |
| FR-07-25 | 應提供熱鍵測試按鈕，於設定面板中直接觸發以驗證行為 | M |

---

### FR-11 場景、背景與光照

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-11-01 | 系統應自 `Backgrounds/` 資料夾讀取使用者自訂背景，並提供「重新掃描」按鈕（不得要求重啟應用程式） | M |
| FR-11-02 | 應支援影像格式 `.jpg`、`.png`（含帶透明通道之 PNG） | M |
| FR-11-03 | 應支援影片背景 `.mp4`、`.webm`，並提供循環播放與靜音播放 | S |
| FR-11-04 | 應提供**純色背景**與色彩選擇器 | M |
| FR-11-05 | 應提供**透明背景**模式（Alpha = 0），為串流輸出之預設模式 | M |
| FR-11-06 | 背景應提供縮放模式：填滿（Cover）、符合（Contain）、拉伸（Stretch） | S |
| FR-11-07 | 應可透過 `ChangeBackground` 熱鍵切換背景 | M |
| FR-11-08 | 應提供「**記住場景**」設定：重啟後還原上次之模型、背景、光照與攝影機位置 | S |
| FR-11-09 | 應提供「**鎖定模型位置**」開關，防止拖曳時誤移動模型（臉部追蹤驅動之位移仍正常作用） | M |
| FR-11-10 | 應可以滑鼠拖曳移動模型、滾輪縮放、右鍵拖曳旋轉；操作須可由 FR-11-09 鎖定 | M |
| FR-11-11 | **光照系統：** 應提供環境光（顏色 + 強度）與方向光（顏色 + 強度 + 方位角 + 仰角），並即時預覽 | M |
| FR-11-12 | 應提供**邊緣光（Rim Light）**顏色與強度設定，用於使模型自背景中分離 | S |
| FR-11-13 | 應提供 MToon 材質之全域調整：陰影邊界（Shading Shift）、陰影柔和度（Shading Toony） | S |
| FR-11-14 | 光照設定應可儲存為**光照預設集**並由 `ChangeLighting` 熱鍵切換 | S |
| FR-11-15 | 應提供抗鋸齒（MSAA / FXAA）開關與渲染解析度倍率（0.5×–2×），供效能調節 | S |
| FR-11-16 | 應提供陰影開關（接收／投射），預設關閉以節省效能 | C |

---

### FR-13 音效系統

#### FR-13-A 基礎

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-13-01 | 應支援 `.wav`、`.mp3`、`.ogg` 格式 | S |
| FR-13-02 | 音效檔案應位於 `Sounds/` 資料夾或模型同層之音效子資料夾內 | S |
| FR-13-03 | 音檔應於首次載入或首次播放時於背景載入記憶體；載入後保留至應用程式重啟 | S |
| FR-13-04 | 應提供全域播放音量控制與 VU 音量表 | S |
| FR-13-05 | 應提供輸出裝置選擇（預設裝置或指定裝置，供輸出至虛擬音效線） | S |

#### FR-13-B 觸發方式

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-13-10 | **熱鍵音效：** 所有熱鍵類型皆應可附掛音效 | S |
| FR-13-11 | 單一熱鍵應支援最多 **3 個音效插槽（Sound Slot）**，各具獨立音檔清單與延遲設定，以對應動畫中不同時間點之音效（原產品為 10 個，本版精簡） | S |
| FR-13-12 | 每個插槽應可設定音檔清單，播放時隨機挑選其一；各音檔可設定基礎音量與音高 | S |
| FR-13-13 | 音量、音高、延遲、聲道平衡應可設定為固定值或隨機區間 | S |
| FR-13-14 | 應支援「熱鍵停用時自動停止音效」並可設定淡出時間；僅適用於「表情開關」型熱鍵，且僅於明確切換關閉時生效 | S |
| FR-13-15 | **參數音效觸發：** 應可依輸出目標之數值或運動狀態自動播放音效 | C |

#### FR-13-C 參數觸發類型

本版精簡為 **2 種**觸發類型（原產品為 4 種），每種最多 2 組：

| 觸發類型 | 說明 | 範例 |
| --- | --- | --- |
| **Value Trigger** | 參數值進入指定區間時觸發 | 眨眼表情權重高於 0.9 時播放眨眼音 |
| **Speed Trigger** | 參數變化速度進入指定區間時觸發（速度正規化為 -1～1） | 頭部旋轉速度達 ±0.5 時播放衣物摩擦音 |

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-13-20 | 音效應於數值進入觸發區間之**當幀**播放；同一觸發區間須待數值離開後方可再次觸發 | C |
| FR-13-21 | 所有輸出目標（表情權重與骨骼旋轉）皆應可作為觸發輸入 | C |
| FR-13-22 | 應可設定**次要條件**：僅當指定表情啟用時，該觸發才生效 | C |
| FR-13-23 | 應支援可設為固定值或隨機區間之設定：音量、冷卻時間、延遲、聲道平衡、音高 | C |

#### FR-13-D 音量與輸出

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-13-30 | 音量應為三層相乘：**單一音檔 × 熱鍵／觸發群組 × 全域**（原產品為四層，本版移除「模型／道具」層） | S |
| FR-13-31 | 應提供全域音效靜音切換，並可由熱鍵觸發 | S |
| FR-13-32 | **輸出方式（預設）：** 由應用程式播放至系統音訊裝置，由 OBS 以應用程式音訊擷取接收 | S |
| FR-13-33 | 系統應於文件中說明如何於 OBS 中以「應用程式音訊擷取」隔離本應用程式之音效，避免與桌面音訊混同 | S |
| FR-13-34 | 音效播放應經 LiveRoom 同步至其他參與者（見 FR-18） | C |

---

### FR-15 輸出、擷取與錄製

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-15-01 | **OBS 瀏覽器來源輸出（建議方式）：** 系統應啟動本機 HTTP 伺服器（預設埠 `8790`），提供一個**僅含渲染結果、不含任何 UI**、背景為透明之頁面供 OBS「瀏覽器來源」擷取 | M |
| FR-15-02 | 瀏覽器來源輸出應與主視窗共用同一份追蹤與解算結果，**不得重複執行追蹤運算** | M |
| FR-15-02b | 主視窗失去焦點、被其他視窗遮蔽或最小化時，追蹤、解算與廣播**均須以正常更新率持續執行**。瀏覽器引擎預設會對背景視窗進行節流（計時器降頻、動畫框架停止），必須明確停用；直播情境中使用者本就會操作其他程式，此為必要條件而非最佳化 | M |
| FR-15-03 | 輸出頁面應可經查詢參數指定解析度與 FPS（如 `?w=1920&h=1080&fps=60`） | S |
| FR-15-04 | UI 應顯示可直接複製之輸出網址，並附 OBS 設定步驟提示（含「關閉來源時關閉來源」建議設定） | M |
| FR-15-05 | **視窗擷取輸出：** 主視窗應提供「串流模式」，隱藏所有 UI 疊層並以純色或透明背景呈現，供 OBS 視窗擷取使用 | M |
| FR-15-06 | 系統**不得**要求使用者使用綠幕；所有建議路徑皆應支援透明通道 | M |
| FR-15-07 | **虛擬攝影機：** 本版不自製虛擬攝影機驅動，文件應指引使用者以 OBS 內建之虛擬攝影機輸出 | M |
| FR-15-08 | **Spout2 輸出：** 列為選用擴充，若實作則須支援透明通道且來源名稱為 `VRM2LIVE` | C |
| FR-15-09 | **截圖：** 應可於應用程式內截圖並存為 `.PNG`，支援透明背景 | M |
| FR-15-10 | 截圖應可設定輸出解析度（含高於視窗解析度之超取樣，最高 4×）與是否包含背景 | S |
| FR-15-11 | 截圖預設儲存至使用者「圖片\VRM2LIVE」資料夾，路徑可自訂 | M |
| FR-15-12 | 應提供 `TakeScreenshot` 熱鍵，沿用上次截圖設定 | M |
| FR-15-13 | **影片錄製：** 本版不自製錄影器，文件應指引使用者以 OBS 錄製 | M |
| FR-15-14 | 輸出畫面中不得出現熱鍵按鈕、參數面板、滑鼠游標或任何除錯資訊 | M |
| FR-15-15 | 應提供輸出效能指標（實際渲染 FPS、掉幀計數），僅顯示於主視窗，不進入輸出畫面 | S |

---

### FR-16 手機至電腦串流

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-16-01 | PC 端應可啟動**追蹤資料接收伺服器**：UDP 埠 `39539`（VMC Protocol）與 `49983`（iFacialMocap 相容），並綁定所有可用網路介面 | S |
| FR-16-02 | PC 端應提供**內建手機追蹤頁面**：以 WebSocket 服務（預設埠 `8791`）供手機瀏覽器連入，於手機端執行 MediaPipe 追蹤後回傳參數 | S |
| FR-16-03 | PC 端應提供「顯示連線資訊」功能，列出所有網路介面之 IP 位址，並產生可供手機掃描之 QR Code（內容為手機追蹤頁面網址） | M |
| FR-16-04 | UI 應警示：IPv6 位址通常為公開位址，分享截圖或畫面前應遮蔽 | M |
| FR-16-05 | 手機端應可手動輸入 IP／埠，亦可經 QR Code 直接連入 | M |
| FR-16-06 | 連線中斷時，手機端應自動嘗試重新連線（指數退避，上限 30 秒） | M |
| FR-16-07 | **隱私要求：** 手機端不得將原始影像或原始地標資料傳送至 PC，僅得傳送計算後之參數 | M |
| FR-16-08 | 手機追蹤頁面應提供 8 個熱鍵按鈕（見 FR-07-04）與追蹤開關 | S |
| FR-16-09 | 手機追蹤頁面應顯示連線狀態、實際傳送 FPS 與估計延遲 | S |
| FR-16-10 | 系統應於同時收到多個來源之相同參數時，依 FR-02-07 之來源優先權仲裁，並於 UI 顯示當前生效來源 | S |
| FR-16-11 | 若指定埠已被占用，系統應自動遞增至下一可用埠，並於 UI 明確標示實際使用之埠號 | S |
| FR-16-12 | 系統應提供 Windows 防火牆例外之設定指引；首次啟動伺服器時應偵測並提示 | M |
| FR-16-13 | 追蹤資料協定應為公開文件化之簡單 JSON 或 OSC 格式，允許第三方自製傳送端 | S |
| FR-16-14 | 系統應相容 VSeeFace、Waidayo 等既有 VMC Protocol 傳送端，無需其修改 | S |
| FR-16-15 | 本版**不支援** iOS USB 連線（需 Apple 驅動相依，列為未來擴充） | C |

---

### FR-18 多人協作（LiveRoom）

> **與原產品 VNet 之關鍵差異：** 原產品依賴 Steam P2P、Steam 好友驗證與付費 DLC。本版改為**自架／區網 WebSocket 房間伺服器**，不綁定任何商店平台，且**預設不傳輸模型檔案**（改為要求各端本機已有相同模型），以大幅降低實作複雜度與法律風險。

#### FR-18-A 基本能力

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-18-01 | 系統應內建可由任一參與者啟動之**房間伺服器**（WebSocket，預設埠 `8792`），亦可連線至遠端自架伺服器 | S |
| FR-18-02 | 單一房間最多支援 **4 人**（原產品為 6 人，本版精簡） | S |
| FR-18-03 | 主持人建立房間時應設定：房間名稱、密碼、人數上限、資料傳輸 FPS（10／20／30，預設 20） | S |
| FR-18-04 | 參與者應設定**暱稱**，且介面中不得顯示 IP 位址或任何系統帳號資訊 | M |
| FR-18-05 | **模型來源（預設）：** 各參與者於本機自行載入其 VRM；系統僅同步**姿勢資料**（Humanoid 骨骼旋轉 + 表情權重 + 根節點變換） | S |
| FR-18-06 | 若他人之模型於本機不存在，該參與者應以**佔位替身**顯示，並提示「缺少模型」 | S |
| FR-18-07 | **模型傳輸（選用）：** 應提供明確之選擇加入（opt-in）開關，允許將自身模型傳送給房間成員；開啟前須顯示 VRM 授權旗標（FR-01-05）與再散布警語，並要求二次確認 | S |
| FR-18-08 | 系統應提供「協作控制」畫面，可於直播中安全開啟——僅顯示暱稱、延遲、模型載入狀態，不含任何個資 | M |
| FR-18-09 | 應顯示各參與者之 ping 與模型傳輸進度（若啟用 FR-18-07） | S |
| FR-18-10 | 主持人離開時房間立即結束，所有人斷線並收到明確提示 | S |
| FR-18-11 | 主持人應可將任一參與者踢出並加入封鎖名單 | S |

#### FR-18-B 互動與呈現

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-18-20 | 應可於本機自由擺放各參與者模型之位置、旋轉與縮放，不影響他人畫面 | S |
| FR-18-21 | 應提供「同步佈局」模式，由主持人指定所有人之站位並同步至所有端 | C |
| FR-18-22 | 應可逐參與者顯示／隱藏其模型（用於減輕非直播端之運算負擔） | S |
| FR-18-23 | 應可逐參與者設定**動作資料緩衝**（0–1 秒，預設 0.05 秒）以消除網路抖動 | S |
| FR-18-24 | 應可逐參與者靜音／解除靜音其音效 | C |
| FR-18-25 | 遠端參與者之模型應於本機執行 SpringBone 運算，不透過網路同步搖動骨骼 | S |
| FR-18-26 | 應提供關閉「參數平滑」之選項，以正確呈現他人之逐格動畫或二值表情 | S |
| FR-18-27 | 應提供詳細連線日誌開關，供除錯使用 | S |

#### FR-18-C 安全需求

| 編號 | 需求描述 | 優先 |
| --- | --- | --- |
| FR-18-40 | 密碼錯誤者之連線必須被拒絕；伺服器須對同一來源實施連線速率限制（如 5 次／分鐘）以防暴力嘗試 | M |
| FR-18-41 | 所有連線必須使用 **WSS（TLS）**；自架情境應提供自簽憑證產生工具並於首次連線顯示指紋供比對 | M |
| FR-18-42 | 參與者之 IP 位址不得洩漏給其他參與者；所有資料須經伺服器轉發，不得建立點對點直連 | M |
| FR-18-43 | 若啟用模型傳輸（FR-18-07），傳輸內容必須以 **AES-256** 加密，金鑰僅於房間內分送 | M |
| FR-18-44 | 接收端之他人模型必須**僅存於記憶體**，不得寫入硬碟 | M |
| FR-18-45 | 房間結束時，伺服器須立即刪除所有暫存之模型資料 | M |
| FR-18-46 | 系統不得將任何協作資料、模型或識別資訊傳送至開發者控制之伺服器 | M |
| FR-18-47 | 文件必須明確告知：協作參與者可能以專用工具自記憶體擷取模型，**切勿與不信任之對象啟用模型傳輸** | M |
| FR-18-48 | 房間伺服器應對單一連線之訊息速率與封包大小設限，防止惡意端造成阻斷服務 | M |

#### FR-18-D 限制

| 項目 | 限制值 |
| --- | --- |
| 單一房間人數 | ≤ 4 人 |
| 姿勢資料頻寬 | 每人約 30–80 kb/s（20 FPS、55 骨骼 + 表情） |
| 模型傳輸單檔大小 | ≤ 100 MB（啟用 FR-18-07 時） |
| 表情同步數量 | ≤ 64 個表情群組，超出者不同步 |
| 房間存續上限 | 12 小時，逾時自動結束 |
| 不同步項目 | 背景、光照、音效觸發（除 FR-13-34）、截圖設定 |

---

## 5. 非功能需求

### 5.1 效能需求

| 編號 | 需求描述 |
| --- | --- |
| NFR-P-01 | 於建議硬體上，載入單一 VRM 模型時應穩定維持 60 FPS 渲染，且 CPU 佔用不超過單核心之 50% |
| NFR-P-02 | 追蹤運算應與渲染執行於不同執行緒／工作者，追蹤降速不得造成渲染掉幀 |
| NFR-P-03 | 追蹤資料應內插至應用程式 FPS（約 60 FPS） |
| NFR-P-04 | 自追蹤取樣至畫面呈現之端到端延遲應 ≤ 100 ms（webcam 本機情境） |
| NFR-P-05 | OBS 瀏覽器來源輸出不得重複執行追蹤運算（見 FR-15-02） |
| NFR-P-06 | 模型載入時間：50 MB 以內之 VRM 應於 5 秒內完成載入並顯示 |
| NFR-P-07 | 貼圖與模型應於首次載入後快取，模型切換回既有模型應為即時 |
| NFR-P-08 | 4 人協作時之額外 CPU 負載應大致等同於本機載入 4 個模型；UI 須提供隱藏他人模型以降載之選項 |
| NFR-P-09 | 應用程式閒置（未載入模型）時之記憶體佔用應 ≤ 500 MB |
| NFR-P-10 | 啟用半身或全身追蹤（FR-02-D）後，渲染 FPS 之下降幅度不得超過 20%；姿態與手部估計須可獨立降頻（FR-02-70），且其降速不得造成臉部追蹤或渲染掉幀 |
| NFR-P-11 | 身體追蹤模型檔（姿態、手部）應於首次啟用時載入並常駐，模式切換不得重新載入；停用後可保留於記憶體以利快速再啟用 |

### 5.2 安全與隱私需求

| 編號 | 需求描述 |
| --- | --- |
| NFR-S-01 | 系統不得向任何外部伺服器傳送個人資料、模型或使用統計 |
| NFR-S-02 | 攝影機影像絕不得於介面中顯示，且程式碼中不得存在可實現該行為之路徑 |
| NFR-S-03 | 手機至 PC 之傳輸僅得包含計算後之參數，不得包含影像或原始地標資料 |
| NFR-S-04 | 日誌為純文字、儲存於本機、非經使用者主動操作不得外傳；日誌中不得記錄 IP 位址與檔案完整路徑 |
| NFR-S-05 | LiveRoom 須符合 FR-18-C 全部要求 |
| NFR-S-06 | 本機 HTTP／WebSocket 服務預設僅綁定 `127.0.0.1`；需對外提供者（手機串流、LiveRoom）須由使用者明確啟用 |
| NFR-S-07 | 熱鍵 HTTP 端點須預設關閉，啟用時強制要求權杖 |
| NFR-S-08 | 顯示本機 IP 之畫面須提醒 IPv6 可能為公開位址 |
| NFR-S-09 | 應用程式不得要求管理員權限執行 |

### 5.3 可靠性與資料完整性

| 編號 | 需求描述 |
| --- | --- |
| NFR-R-01 | 設定變更須即時寫入；系統另須每 5 分鐘自動建立備份至 `Backups/`，保留最近 10 份 |
| NFR-R-02 | 所有設定檔須為人類可讀之未加密 JSON，便於使用者自行檢視與救援 |
| NFR-R-03 | 設定檔寫入須為原子操作（先寫暫存檔再更名），避免寫入中斷造成損毀 |
| NFR-R-04 | 損毀或版本不符之設定檔須能被偵測，並在提示後以預設值重建，不得導致當機 |
| NFR-R-05 | 追蹤來源中斷（拔除 webcam、手機離線）不得造成當機，應切換至「追蹤遺失」行為並持續嘗試恢復 |
| NFR-R-06 | 載入毀損或非 VRM 之 `.vrm` 檔案須以明確錯誤訊息告知，不得當機 |
| NFR-R-07 | 唯讀之模型設定檔不得導致當機，應以明確訊息告知使用者 |

### 5.4 相容性需求

| 編號 | 需求描述 |
| --- | --- |
| NFR-C-01 | 應支援 VRM 0.x 與 VRM 1.0 兩代規格 |
| NFR-C-02 | 應相容 VRoid Studio、VRoid Hub、BOOTH 常見之 VRM 匯出結果 |
| NFR-C-03 | 應相容 OBS Studio 之瀏覽器來源、視窗擷取、應用程式音訊擷取與虛擬攝影機 |
| NFR-C-04 | 應相容 VMC Protocol 傳送端（VSeeFace、Waidayo 等） |
| NFR-C-05 | 對於缺少非必要 Humanoid 骨骼（如 `leftEye`、`chest`、`upperChest`、肩膀、腳趾、手指）之模型，須能正常載入並以替代方案驅動（見 FR-02-77） |
| NFR-C-06 | 應相容 Stream Deck 等外部工具（透過 FR-07-05 之 HTTP 端點） |

### 5.5 可用性與在地化

| 編號 | 需求描述 |
| --- | --- |
| NFR-U-01 | 介面須至少支援繁體中文與英文切換 |
| NFR-U-02 | 首次啟動須提供引導流程：選擇模型 → 選擇追蹤來源 → 執行 Auto-Setup → 顯示輸出網址 |
| NFR-U-03 | 危險或不可逆操作（重設全部映射、刪除表情、啟用模型傳輸）須有二次確認 |
| NFR-U-04 | 涉及第三方權益之操作（模型傳輸、截圖分享）須顯示模型授權摘要與尊重原作者之提示 |
| NFR-U-05 | 所有數值滑桿須同時提供數值輸入框與「重設為預設值」 |

### 5.6 可維護性與擴充性

| 編號 | 需求描述 |
| --- | --- |
| NFR-M-01 | 追蹤來源、輸出目標型態、熱鍵動作應以可擴充之註冊表定義，新增項目不得需要修改核心解算迴圈 |
| NFR-M-02 | 設定檔須含結構版本號，並提供跨版本遷移機制 |
| NFR-M-03 | 追蹤協定、熱鍵動作列舉、設定檔結構應公開文件化 |
| NFR-M-04 | 應提供可重播之追蹤資料錄製檔（`.vrmtrace.json`），供無攝影機環境下之自動化測試使用 |

---

## 6. 外部介面需求

### 6.1 檔案格式介面

| 類別 | 支援格式 |
| --- | --- |
| 3D 模型 | `.vrm`（VRM 0.x 與 1.0，皆為 glTF 2.0 二進位容器） |
| 模型設定 | `<模型名>.vrmlive.json` |
| 映射預設 | `.vrmmap.json` |
| 動畫 | `.vrma`（主要）、`.bvh`（次要） |
| 背景（影像） | `.jpg`、`.png` |
| 背景（影片） | `.mp4`、`.webm` |
| 音效 | `.wav`、`.mp3`、`.ogg` |
| 模型縮圖 | `.png`、`.jpg` |
| 截圖輸出 | `.png`（支援透明背景） |
| 追蹤錄製（測試用） | `.vrmtrace.json` |

### 6.2 網路埠與通訊協定

| 用途 | 協定 | 預設埠 | 預設狀態 | 綁定 |
| --- | --- | --- | --- | --- |
| OBS 輸出頁面 | HTTP | 8790 | 開啟 | `127.0.0.1` |
| 手機追蹤頁面／資料 | HTTP + WebSocket | 8791 | 關閉 | `0.0.0.0`（啟用後） |
| LiveRoom 房間伺服器 | WSS | 8792 | 關閉 | `0.0.0.0`（啟用後） |
| VMC Protocol 接收 | OSC over UDP | 39539 | 關閉 | `0.0.0.0`（啟用後） |
| iFacialMocap 相容接收 | UDP | 49983 | 關閉 | `0.0.0.0`（啟用後） |
| 熱鍵 HTTP 端點 | HTTP | 8790（同輸出） | 關閉 | `127.0.0.1` |

> 所有埠皆可於設定中變更；被占用時自動遞增並於 UI 標示實際埠號（FR-16-11）。

### 6.3 資料夾結構

```
<應用程式路徑>/
├── VRM2LIVE.exe
├── Models/                  ← 使用者 VRM 模型
│   ├── 白_shiro_.vrm
│   └── 白_shiro_.vrmlive.json   ← 自動產生
├── Animations/              ← .vrma / .bvh
├── Backgrounds/             ← 使用者背景
├── Sounds/                  ← 音效
├── Backups/                 ← 設定自動備份（保留 10 份）
├── Logs/                    ← 純文字日誌
└── Config/
    ├── app_config.json           ← 全域設定
    ├── tracking_calibration.json ← 追蹤校準
    ├── lighting_presets.json     ← 光照預設集
    └── liveroom.json             ← LiveRoom 設定與封鎖名單
```

---

## 7. 資料模型與設定檔

| 檔案 | 內容 | 特性 |
| --- | --- | --- |
| `<模型>.vrmlive.json` | 參數映射、表情定義與群組、熱鍵、動畫指派與骨骼遮罩、變換與攝影機預設、SpringBone 倍率、音效觸發 | 人類可讀 JSON、未加密、含結構版本號、自動存檔與備份 |
| `app_config.json` | 全域設定（介面語言、追蹤來源與優先權、身體追蹤模式與逐部位設定、渲染 FPS、輸出埠、音訊裝置、記住場景） | 人類可讀 JSON |
| `tracking_calibration.json` | 各追蹤來源之中性原點校準資料（含身體追蹤之中性姿勢，FR-02-76） | 跨重啟持久化 |
| `lighting_presets.json` | 光照預設集 | 與模型解耦，可跨模型共用 |
| `liveroom.json` | 暱稱、伺服器位址、封鎖名單、緩衝設定 | **不得**儲存房間密碼與加密金鑰 |
| `*.vrmmap.json` | 可攜式映射預設集 | 供跨模型／跨機器共用 |

> **設計原則：** 所有產生之設定檔皆為人類可讀且未加密，使用者可自行檢視與修正；UI 須提示手動編輯有損毀風險，且系統須具備損毀復原能力（NFR-R-04）。
>
> **不可變原則：** 系統**永不**寫入或修改原始 `.vrm` 檔案。所有客製化一律外置於 `.vrmlive.json`。

---

## 8. 限制、假設與範圍外項目

### 8.1 產品邊界

| 編號 | 限制 |
| --- | --- |
| L-01 | **不支援 Live2D 模型**；此為明確之產品定位而非缺陷（與藍本產品互為鏡像） |
| L-02 | 僅支援 Windows；macOS／Linux 於本版不驗證（Electron 技術上可移植，但全域熱鍵與音訊擷取路徑需重寫） |
| L-03 | 不提供原生行動版 App；手機僅作為追蹤來源（經瀏覽器頁面或第三方 App） |
| L-04 | 不自製虛擬攝影機與錄影器，改以 OBS 既有能力達成（FR-15-07、FR-15-13） |
| L-05 | Spout2／NDI 輸出為選用（優先級 C），簡易版之建議路徑為 OBS 瀏覽器來源 |
| L-06 | 半身／全身追蹤（FR-02-D）以**單一攝影機**之視覺姿態估計實現：無 IK 反向運動學、無 VR 裝置與外部動捕硬體支援。沿鏡頭光軸之前後深度本質上不精確；下半身位於畫面外或桌面下時，`full` 模式無法取得有效資料 |
| L-07 | 手指追蹤僅估計各指節之**彎曲方向**，不估計指節扭轉；雙手交疊、握拳遮蔽或快速移動時精度顯著下降。`face` 模式下手指姿勢仍僅能經動畫或靜態姿勢驅動 |
| L-08 | Webcam 追蹤不支援舌頭伸出與臉頰鼓起；此類參數僅於 ARKit 來源（FR-02-05）可用 |
| L-09 | LiveRoom 不同步背景、光照與音效觸發；各端呈現可能不同 |

### 8.2 資源與規模限制

| 編號 | 限制 |
| --- | --- |
| L-20 | 畫面按鈕熱鍵最多 8 個 |
| L-21 | 單一熱鍵最多 3 個音效插槽；每種參數音效觸發類型最多 2 組 |
| L-22 | 單一熱鍵最多串接 4 個動作 |
| L-23 | LiveRoom 各項限制詳見 FR-18-D |
| L-24 | 音檔載入後常駐記憶體至重啟；大量大型音檔可能造成記憶體累積 |
| L-25 | 同時載入之模型數上限為 4（含 LiveRoom 遠端參與者） |
| L-26 | 身體追蹤同時僅支援 **1 人**；同時偵測之手部上限為 **2 隻** |
| L-27 | 身體追蹤不同步至 LiveRoom 以外之任何來源；`half`／`full` 模式與 VMC／iFacialMocap 之骨骼資料同時輸入時，依 FR-02-07 之來源優先權擇一，不做混合 |

### 8.3 範圍外之藍本模組

下列原 VTube Studio 模組於本版**明確不實作**，其編號保留以利未來擴充時對照：

| 原編號 | 模組 | 不實作之理由 |
| --- | --- | --- |
| FR-04 | 物理與風力系統 | VRM 內建 SpringBone 已涵蓋核心需求，併入 FR-01-13；風力列為未來擴充 |
| FR-08 | 道具系統 | 3D 場景中之道具需額外之附著點與碰撞設計，複雜度高 |
| FR-09 | Web Items | 需嵌入瀏覽器引擎與完整沙箱安全模型 |
| FR-10 | ArtMesh 客製化 | Live2D 專屬概念；VRM 對應需求已由表情之材質綁定涵蓋（見 FR-05 附註） |
| FR-12 | 視覺特效（VFX） | 後製特效鏈為獨立子系統 |
| FR-14 | 螢幕光照覆蓋 | 需螢幕擷取權限與原生 API，且有隱私風險 |
| FR-17 | Twitch 整合 | 需 OAuth、EventSub 與長期權杖管理 |
| FR-19 | Steam 創意工坊 | 需綁定商店平台 |
| FR-20 | 外掛 API | 本版僅提供最小化之熱鍵 HTTP 端點（FR-07-05） |
| FR-21 | Cubism Editor 整合 | Live2D 專屬 |
| FR-22 | 應用程式一般設定 | 併入各模組與 `app_config.json` |

### 8.4 假設

| 編號 | 假設 |
| --- | --- |
| A-01 | 使用者已具備可正常運作之 `.vrm` 模型，且對其具有合法使用權 |
| A-02 | 使用者已安裝 OBS Studio（本機已確認安裝） |
| A-03 | 手機與 PC 位於同一區域網路（手機串流情境） |
| A-04 | 使用者具備必要之防火牆權限，或知悉如何設定例外 |
| A-05 | 開發環境需另行安裝 Node.js 20 LTS 以上（本機目前未安裝） |
| A-06 | LiveRoom 參與者彼此信任，或已知悉 FR-18-47 之風險告知 |

---

## 9. 開發里程碑與驗收準則

### 9.1 里程碑

| 階段 | 範圍 | 完成定義 |
| --- | --- | --- |
| **M1：能動** | FR-01（載入／Auto-Setup）、FR-02-01／20–30、FR-03（核心）、FR-15-01／05 | 以 webcam 驅動 `白_shiro_.vrm` 之頭部與眼睛，並能由 OBS 瀏覽器來源以透明背景擷取 |
| **M2：能播** | FR-05、FR-06、FR-07、FR-11 | 可用熱鍵切換表情、播放動畫、切換背景與光照 |
| **M2.5：能動全身** | FR-02-D（含 FR-02-09、FR-02-60～79） | 以同一支 webcam 驅動模型之軀幹、雙臂與十指；`full` 模式可驅動雙腿；三種模式可即時切換且淡接無跳動 |
| **M3：能調** | FR-02（完整）、FR-03（完整）、FR-13、FR-15（完整） | 語音口型、逐項映射調校、音效、截圖齊備 |
| **M4：能連** | FR-16、FR-18 | 手機可作為追蹤來源；4 人可同框 |

### 9.2 關鍵驗收準則

| 編號 | 驗收條件 | 對應需求 |
| --- | --- | --- |
| AC-01 | 載入 `白_shiro_.vrm`（VRM 0.x，30 MB，38 個表情群組）後，Auto-Setup 須自動建立至少：頭部三軸旋轉、雙眼開闔、五母音、LookAt 共 11 組映射 | FR-01-09 |
| AC-02 | 遮住臉部再露出，模型須依「追蹤遺失時行為」設定平順過渡，不得瞬跳 | FR-02-25 |
| AC-03 | 對同一輸出目標重複指派輸入時，UI 須阻擋並指出既有指派 | FR-03-04 |
| AC-04 | 播放僅含下半身遮罩之動畫時，頭部須仍由臉部追蹤驅動 | FR-06-09 |
| AC-05 | 表情啟用中播放一次性動畫，動畫結束後表情須維持啟用且無跳動 | FR-03-11 |
| AC-06 | OBS 瀏覽器來源擷取結果須具正確透明通道，且不含任何 UI 元素或滑鼠游標 | FR-15-01、FR-15-14 |
| AC-07 | 應用程式關閉後重啟，模型、背景、光照、攝影機位置與所有映射須完整還原 | FR-11-08、NFR-R-01 |
| AC-08 | 手動破壞 `.vrmlive.json`（寫入非法 JSON）後啟動，須顯示明確錯誤並以預設值重建，不得當機 | NFR-R-04 |
| AC-09 | 全程封包側錄須確認：手機端未傳送任何影像資料；應用程式未連線任何外部網域 | FR-16-07、NFR-S-01 |
| AC-10 | 以錯誤密碼嘗試加入 LiveRoom 6 次，第 6 次須因速率限制被拒 | FR-18-40 |
| AC-11 | 端到端延遲（webcam 情境）實測須 ≤ 100 ms | NFR-P-04 |
| AC-12 | 原始 `.vrm` 檔案之雜湊值於完整使用流程前後須維持一致 | §7 不可變原則 |
| AC-13 | 切換至 `half` 模式後舉起單手，模型須抬起**對應側**之手臂（依鏡像設定），且十指之彎曲須跟隨；切回 `face` 模式後手臂須於淡接時間內平順回到基準待機姿勢 | FR-02-61、FR-02-62、FR-02-68、FR-02-74 |
| AC-14 | 於 `half` 或 `full` 模式播放帶「手臂」骨骼遮罩之一次性動畫時，該動畫必須完整覆寫身體追蹤之手臂姿態；動畫結束後手臂須平順交還給身體追蹤 | FR-02-66、FR-06-09 |
| AC-15 | 對缺少 `upperChest` 與肩膀骨骼之模型啟用 `half` 模式，須能正常運作且不報錯，其旋轉份額併入 `chest`／`spine` | FR-02-77、NFR-C-05 |

---

## 10. 附錄

### 附錄 A-1：輸入參數對照表

| 參數名稱 | 說明 | 範圍 | Webcam | 手機頁面 | ARKit／VMC |
| --- | --- | :-: | :-: | :-: | :-: |
| `FacePositionX` | 臉部水平位置 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `FacePositionY` | 臉部垂直位置 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `FacePositionZ` | 與鏡頭距離 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `FaceAngleX` | 頭部左右旋轉（Yaw） | -30 ～ 30 度 | ✔ | ✔ | ✔ |
| `FaceAngleY` | 頭部上下旋轉（Pitch） | -30 ～ 30 度 | ✔ | ✔ | ✔ |
| `FaceAngleZ` | 頭部左右傾斜（Roll） | -30 ～ 30 度 | ✔ | ✔ | ✔ |
| `EyeOpenLeft` / `EyeOpenRight` | 左／右眼張開程度 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `EyeLeftX` / `EyeLeftY` | 左眼視線 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `EyeRightX` / `EyeRightY` | 右眼視線 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `BrowLeftY` / `BrowRightY` | 左／右眉上下 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `Brows` | 雙眉合併上下 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `MouthOpen` | 張嘴程度 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `MouthSmile` | 微笑程度 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `MouthX` | 嘴部左右位移 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `CheekPuff` | 鼓臉頰 | 0 ～ 1 | ✘ | ✘ | ✔ |
| `TongueOut` | 伸舌 | 0 ～ 1 | ✘ | ✘ | ✔ |
| `MousePositionX` / `MousePositionY` | 滑鼠位置 | -1 ～ 1 | ✔ | ✔ | ✔ |
| `VoiceVolume` | 麥克風音量 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `VoiceFrequency` | 依偵測音素計算 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `VoiceA` / `VoiceI` / `VoiceU` / `VoiceE` / `VoiceO` | 母音偵測 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `VoiceSilence` | 靜音偵測 | 0 ～ 1 | ✔ | ✔ | ✔ |
| `ARKit_*`（52 項） | 原始 blendshape 係數直通 | 0 ～ 1 | ✔ | ✔ | ✔ |

> 語音類參數之來源為 PC 或手機端麥克風，與臉部追蹤來源無關，故三欄皆可用。
>
> **身體追蹤（FR-02-D）之骨骼姿態不列於本表。** 其輸出為逐骨骼之四元數而非純量，無法以本表之「區間映射」語意表達，故以獨立通道直寫骨骼（FR-02-64），對照關係見附錄 A-6。

### 附錄 A-2：VRM 表情預設與 Auto-Setup 對照

| VRM 預設名稱（0.x / 1.0） | Auto-Setup 來源 | 說明 |
| --- | --- | --- |
| `neutral` | — | 中性，不映射 |
| `a` / `i` / `u` / `e` / `o` | `VoiceA`…`VoiceO`（可混合 `MouthOpen`） | 五母音口型 |
| `blink` | `min(EyeOpenLeft, EyeOpenRight)` 反向 | 雙眼連動眨眼 |
| `blink_L` / `blink_R`（1.0 為 `blinkLeft` / `blinkRight`） | `EyeOpenLeft` / `EyeOpenRight` 反向 | 單眼眨眼；模型缺此預設時退化為 `blink` |
| `lookUp` / `lookDown` / `lookLeft` / `lookRight` | `EyeLeftY` / `EyeRightX` 等 | **僅於** `lookAtTypeName` 為 `BlendShape` 時使用；為 `Bone` 時改驅動 `leftEye` / `rightEye` 骨骼 |
| `happy` / `angry` / `sad` / `relaxed` / `surprised`（1.0） | — | 不自動映射，供熱鍵手動觸發 |
| 自訂群組（如 `01-prefab_happy01`） | — | 不自動映射，列於表情面板供手動指派 |

### 附錄 A-3：Humanoid 骨骼映射預設

| 輸入 | 骨骼 | 軸 | 預設輸出區間 |
| --- | --- | --- | --- |
| `FaceAngleX` | `head`（30%）+ `neck`（40%）+ `spine`（30%） | Y | -30 ～ 30 度 |
| `FaceAngleY` | `head`（40%）+ `neck`（40%）+ `chest`（20%） | X | -25 ～ 25 度 |
| `FaceAngleZ` | `head`（50%）+ `neck`（50%） | Z | -20 ～ 20 度 |
| `FacePositionX` | 模型根節點 | 平移 X | -0.2 ～ 0.2 公尺 |
| `FacePositionY` | 模型根節點 | 平移 Y | -0.1 ～ 0.1 公尺 |
| `EyeLeftX` / `EyeRightX` | `leftEye` / `rightEye` | Y | -12 ～ 12 度 |
| `EyeLeftY` / `EyeRightY` | `leftEye` / `rightEye` | X | -8 ～ 8 度 |
| （自動呼吸） | `chest` | X | 0 ～ 2 度，週期 4 秒 |

> **分配比例（%）** 用於將單一輸入之旋轉量分散至多節骨骼，避免頭部單獨大幅轉動造成頸部斷裂感。比例應可由使用者調整。模型缺少 `chest` 時，其份額須自動併入 `spine`（見 NFR-C-05）。

### 附錄 A-4：範例模型分析（`白_shiro_.vrm`）

本資料夾內附之範例模型，經解析結果如下，可作為開發期之基準測試素材：

| 項目 | 值 |
| --- | --- |
| 檔案大小 | 30.3 MB |
| glTF 版本 | 2.0（二進位 GLB 容器） |
| VRM 規格 | **0.0（VRM 0.x）**，glTF 擴充為 `VRM` |
| 其他擴充 | `KHR_materials_unlit`、`KHR_texture_transform` |
| 標題 / 版本 / 作者 | `白_shiro` / `1.0.0` / `今天来点蒸汽波` |
| 授權 | `allowedUserName: Everyone`、`commercialUssageName: Disallow`、`licenseName: Redistribution_Prohibited` |
| 網格 / 節點 / 材質 / 貼圖 | 12 / 260 / 14 / 16 |
| Humanoid 骨骼 | 53 個（含雙眼、完整十指、腳趾） |
| 表情群組 | **38 個**——13 個標準預設（`neutral`、`a`–`o`、`blink`、`blink_L`、`blink_R`、`lookUp/Down/Left/Right`）+ 25 個自訂（`happy01–06`、`angry01–05`、`sad01–03`、`awkward01–04`、`doubt`、`startled`、`emoji`、`clothes`、`hat`、`hairpin`、`sleeve`） |
| LookAt 型態 | `Bone`（眼球骨骼驅動，非 BlendShape） |
| SpringBone | 17 個骨骼群組、60 個碰撞體群組 |

**對開發之意涵：**

1. 此模型為 **VRM 0.x**，故 FR-01-02 之雙版本支援於首階段即須具備（不可僅實作 1.0）。
2. `lookAtTypeName` 為 `Bone`，故 Auto-Setup 須走骨骼路徑而非表情路徑（見附錄 A-2），此為 FR-01-10 退化邏輯之實測案例。
3. 具備 `blink_L` / `blink_R`，可完整驗證 FR-02-26 之眨眼連動三種模式。
4. 25 個自訂表情（含 `clothes`／`hat`／`hairpin`／`sleeve` 等換裝類）為 FR-05-12 表情群組互斥與 FR-05-14 反轉成員之理想測試素材。
5. 授權為 `Redistribution_Prohibited` 且禁止商業使用，正可作為 FR-01-06 與 FR-18-07 授權警語之驗證案例——系統須在啟用 LiveRoom 模型傳輸時明確攔阻並提示。
6. 17 組 SpringBone 與 60 組碰撞體屬中上規模，適合作為 NFR-P-01 之效能基準。

### 附錄 A-5：術語對照（藍本 → 本版）

| VTube Studio（Live2D） | VRM2LIVE（VRM） |
| --- | --- |
| `.moc3` + `.model3.json` + 貼圖資料夾 | 單一 `.vrm` 檔 |
| `<模型>.vtube.json` | `<模型>.vrmlive.json` |
| Live2D 參數（`ParamAngleX`） | Humanoid 骨骼旋轉軸 + 表情權重 |
| ArtMesh | glTF Primitive／Material |
| `.exp3.json` 表情 | VRM Expression／BlendShapeGroup |
| `.motion3.json` 動畫 | `.vrma`（VRM Animation）／`.bvh` |
| `physics3.json` 物理 | VRM SpringBone |
| Cubism SDK 渲染 | three.js + three-vrm（MToon） |
| Spout2 / NDI 輸出 | OBS 瀏覽器來源（HTTP + 透明通道） |
| VNet（Steam P2P、付費 DLC） | LiveRoom（自架 WebSocket 房間） |
| 道具（Item / Prop） | （本版不實作） |

### 附錄 A-6：身體追蹤地標與骨骼對照（FR-02-D）

**座標轉換.** 姿態地標之座標系為「影像座標」：x 向影像右方、y 向下、z 之絕對值越小表示越接近鏡頭。
模型側之基底則於載入時自模型量測（FR-02-65）：以左右上臂節點連線為 X 軸（指向角色左側）、
`hips` 至 `head` 連線為 Y 軸（向上）、兩者外積為 Z 軸（角色正面）。轉換式為

```
v模型 = X軸 · (s · x地標) + Y軸 · (−y地標) + Z軸 · (−z地標)      s = 鏡像時 −1，否則 +1
```

鏡像開啟時除了將 x 分量取負，左右地標之角色歸屬亦須對調——兩者缺一即為錯誤結果（FR-02-74）。

**骨骼求解.** 每根骨骼取其「rest pose 骨節方向 → 目標骨節方向」之最小旋轉，
再以父骨骼之已解算世界旋轉取逆，換算為局部旋轉。

| 骨骼 | 骨節方向來源（起點 → 終點） | 模式 |
| --- | --- | --- |
| `hips` | 左髖 → 右髖（連線方向）＋ 髖中點 → 肩中點 | half / full |
| `spine`／`chest`／`upperChest` | 髖中點 → 肩中點，依比例分配（預設 40%／35%／25%） | half / full |
| `leftShoulder`／`rightShoulder` | 肩中點 → 同側肩 | half / full |
| `leftUpperArm`／`rightUpperArm` | 同側肩 → 同側肘 | half / full |
| `leftLowerArm`／`rightLowerArm` | 同側肘 → 同側腕（並承接手腕扭轉之一部分） | half / full |
| `leftHand`／`rightHand` | 手部地標之腕 → 中指掌指關節，扭轉取自掌面法向 | half / full |
| 十指共 30 根骨骼 | 見下表 | half / full |
| `leftUpperLeg`／`rightUpperLeg` | 同側髖 → 同側膝 | full |
| `leftLowerLeg`／`rightLowerLeg` | 同側膝 → 同側踝 | full |
| `leftFoot`／`rightFoot` | 同側踝 → 同側腳尖 | full |
| 根節點垂直位移 | 肩中點高度相對中性姿勢之變化量 | full（可關閉） |

**手指對照.** 手部地標每手 21 點，索引與骨骼之對應如下（`{side}` 為 `left`／`right`）：

| 手指 | 骨骼（VRM 1.0 命名） | 地標索引（起點 → 終點） |
| --- | --- | --- |
| 拇指 | `{side}ThumbMetacarpal` / `Proximal` / `Distal` | 1→2、2→3、3→4 |
| 食指 | `{side}IndexProximal` / `Intermediate` / `Distal` | 5→6、6→7、7→8 |
| 中指 | `{side}MiddleProximal` / `Intermediate` / `Distal` | 9→10、10→11、11→12 |
| 無名指 | `{side}RingProximal` / `Intermediate` / `Distal` | 13→14、14→15、15→16 |
| 小指 | `{side}LittleProximal` / `Intermediate` / `Distal` | 17→18、18→19、19→20 |

> VRM 0.x 之拇指命名為 `Proximal`／`Intermediate`／`Distal`，與 VRM 1.0 之
> `Metacarpal`／`Proximal`／`Distal` 錯開一節。實作須同時查表兩種命名，
> 取模型實際具備者（FR-02-77）。

---

## 需求彙總統計

| 模組 | 需求數 | 主要優先級 |
| --- | --- | --- |
| FR-01 模型管理 | 24 | M |
| FR-02 臉部與身體追蹤 | 48 | M / S |
| FR-03 參數映射引擎 | 15 | M |
| FR-05 表情系統 | 19 | M / S |
| FR-06 動畫系統 | 14 | M / S |
| FR-07 熱鍵系統 | 19 + 15 種動作 | M / S |
| FR-11 場景、背景與光照 | 16 | M / S |
| FR-13 音效系統 | 21 | S / C |
| FR-15 輸出、擷取與錄製 | 16 | M |
| FR-16 手機至電腦串流 | 15 | M / S |
| FR-18 多人協作 | 26 | S（安全項為 M） |
| **功能需求合計** | **232** | — |
| NFR 非功能需求 | 39 | — |
| 限制與假設 | 27 | — |
| 驗收準則 | 15 | — |

> 相較藍本文件之 22 個模組、約 470 項功能需求，本簡易版收斂至 **11 個模組、232 項需求**。

---

*文件結束*
